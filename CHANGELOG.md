# 变更日志（CHANGELOG）

## [v1.9.17] - 2026-09-08

### 工作计划编排智能体（plan.html）取消「生成正式文档」步骤

- **步骤④直达导出**：应用户要求，取消「生成正式文档」中间步骤——进入步骤④即为成稿状态，右上角恒为「导出」主按钮（Word .doc），不再有生成前的 genDoc 按钮与生成后的文字提示两种状态切换。
- **状态清理**：`state.generatedDoc` 字段及快照/恢复（snapshot/restore）、applyScenario 重置、updateToolbar 导出禁用逻辑、buildDocHtml 落款条件、格式校验「落款单位与成文日期」检查项、decorateIcons genDoc 映射、genDoc()/事件分支全部移除；文稿落款（发文单位+成文日期）改为始终呈现。
- **步骤完成标记**：从步骤③点「生成工作计划」进入步骤④时同步置 stepDone[3]=true，保持步骤条 ✓ 状态一致。
- **质量验证**：JS 语法通过、标签平衡完整、generatedDoc/genDoc/生成正式文档零残留；jsdom 冒烟 29/29 通过、0 报错（新增生成步骤已取消、直达导出、落款呈现断言；另修一处断言误报——脚本注释含目标文案会污染 body.innerHTML 文本匹配）。

## [v1.9.16] - 2026-09-07

### 工作计划编排智能体（plan.html）三项优化

- **「开始智能解析」按钮醒目化**：步骤①底部新增居中主按钮（`.parse-cta`）——生态绿渐变底色（#168447→#43A047）、44px 高度、加粗加大字号、悬停上浮效果，下方附「上传或粘贴资料后即可开始」引导小字；静态 HTML 与动态渲染（renderStep1UploadBody）两处同步替换。计划类型详情面板（标题+章节结构）保持展示。
- **「编制设置」模块删除**：整个 view-settings 视图、左侧导航「编制设置」入口、saveSettings/resetSettings 事件分支、settings 图标映射及 set-group/radio-row/opt 相关 CSS 一并移除；残留检查仅剩注释。
- **「进入格式生成」改名 + 生成页导出**：步骤③底部流转按钮更名为「生成工作计划」；步骤④生成正式文档后，右上角由文字提示改为「导出」主按钮（data-action=exportWord，导出 Word 格式 .doc 文档，逻辑复用既有 exportWord()），并加 data-icon-ready 防止图标覆盖。
- **质量验证**：JS 语法通过、标签平衡完整、settings/进入格式生成等零残留；jsdom 冒烟 28/28 通过、0 报错（smoke-plan.js 扩展至 28 断言，新增 parse-cta 醒目样式、设置模块删除、生成后导出按钮、按钮改名断言）。

## [v1.9.15] - 2026-09-07

### 工作计划编排智能体（plan.html）模板库管理两项优化

- **模板卡片精简**：应用户要求，每个模板仅展示模板名称、模板描述与章节结构；移除「适用场景」「正文字体」「行距」信息表。
- **新建模板弹框重构**：点击右上角「新建模板」弹出新弹框，含模板名称、模板描述、章节结构（每行一个章节标题）三个参数，点击保存即新增为自定义模板，供计划类型编制套用；名称为空或章节为空时拦截提示。
- **「上传自定义模板」按钮删除**：模板库管理页右上角该按钮已移除，同步清理 uploadTplMgr 图标映射与事件分支；步骤④的模板上传入口（uploadTpl）保留。
- **质量验证**：JS 语法通过、uploadTplMgr 残留为 0；jsdom 运行时实测新建模板全流程（弹框三字段 → 保存 → 列表新增第 5 张卡片并显示名称与章节）；冒烟 23/23 通过、0 报错。

## [v1.9.14] - 2026-09-07

### 会议纪要智能生成智能体（agent-workbench/meeting.html）五项优化

- **新增「粘贴文本」页签**：上传资料区页签扩展为「会议录音｜已转写文本｜粘贴文本」三个；粘贴面板为引导态（点击进入输入态），含实时字数统计、不足 50 字拦截、「清空重贴」「确认文本」操作；确认后以「主材料」条目展示，与录音/转写文本互斥；删除后输入区恢复引导态。
- **模板智能匹配回显修复**：主下拉「纪要模板」新增「智能匹配（推荐）」默认选项；「查看模板」弹窗打开时按主下拉当前值回显（含 auto），选智能匹配后「使用该模板」可正常同步回主下拉。
- **会议类型支持自定义**：下拉新增「自定义类型…」选项，选中切换为文本输入（≤20 字校验）并可返回下拉；新增 `getMeetingTypeText()`/`validateMeetingType()` 统一取值校验，转写摘要/结果页/历史记录全部适配自定义名称；自定义类型时模板自动切「智能匹配」。
- **非录音路径跳过转写页**：粘贴文本与已转写文本一样直接进入智能生成进度，不再进入「录音转写标记」视图；结果页转写文本标签三态显示（转写文本/已转写文本/粘贴文本），音频播放器仅在录音模式显示，待办「信息来源」列按模式切换。
- **历史记录状态收敛为三种**：移除「待审核」，仅保留「已完成／生成中／草稿」；「生成中」状态带呼吸圆点动画；生成开始即写入「生成中」历史记录，完成后自动更新为「已完成」。
- **质量验证**：JS 语法通过；jsdom 冒烟测试 40/40 通过、0 报错（脚本 agent-workbench/smoke-meeting.js），覆盖粘贴页签全流程、模板回显、自定义类型校验、非录音路径直连生成、历史状态流转与结果页适配。

## [v1.9.13] - 2026-09-07

### 工作计划编排智能体（plan.html）步骤①四项优化

- **「管理更多模板」入口**：计划类型下拉右侧新增「🗂 管理更多模板」按钮，点击跳转至模板库管理视图。
- **类型切换轻量化**：切换计划类型不再重置页面、不再自动载入示例资料；仅更新类型元数据（标题/文种/模板映射）与详情面板，已上传/粘贴的资料保留，不弹任何弹窗。
- **详情面板精简**：计划详情面板移除「编制单位」「成文模板」两项，仅保留类型标题与章节结构。
- **上传/粘贴改版**：按参考截图重构为左右双列「上传附件（可选）｜粘贴文本（可选）」——左列为白底浅框上传区（云上传图标+「点击或拖拽文件到此处」），已上传文件以「图标+文件名+类型·大小+红色垃圾桶图标」列表呈现在上传区下方；右列为大尺寸无边框粘贴输入区（右下角字数）。原 emoji 图标与虚线大卡片样式弃用。
- **质量验证**：JS 语法通过、标签配平完整（div 256/256）；jsdom 冒烟 23/23 通过、0 报错，覆盖双列结构、管理按钮、类型轻量切换（无弹框/无示例载入/解析按钮状态正确）及四步流转。

## [v1.9.12] - 2026-09-07

### 工作计划编排智能体（plan.html）「格式校验」按钮删除

- **步骤④信息条精简**：应用户要求，移除格式生成环节信息条中的「🔍 格式校验」按钮；该按钮是打开右侧「智能分析」抽屉格式校验页签的唯一入口，删除后步骤④信息条仅保留「生成正式文档」主操作（生成后为「可导出」提示）。
- **功能保留**：右侧抽屉及「要素提取」页签、格式校验渲染逻辑（renderDrawer）均保留；因步骤④已无入口，后续如需可再清理抽屉的校验页签。
- **质量验证**：JS 语法通过、标签配平完整（button 52/52）、目标按钮无残留；jsdom 冒烟 21/21 通过、0 报错。

## [v1.9.11] - 2026-09-07

### 工作计划编排智能体（plan.html）计划类型直接切换

- **移除二次确认弹框**：应用户要求，切换计划类型不再弹出「是否继续」确认框，下拉选择后直接生效；切换时仍会重置编制内容并同步载入该类型示例资料，toast 轻提示确认结果。
- **同步简化**：下拉 change 事件中「确认前回显原类型」的过渡逻辑一并移除。
- **质量验证**：JS 语法通过、标签配平完整；jsdom 实测切换后无确认弹框、下拉值与详情面板即时联动；冒烟 21/21 通过、0 报错。

## [v1.9.10] - 2026-09-07

### 工作计划编排智能体（plan.html）「一键载入示例资料」按钮删除

- **按钮删除**：应用户要求，移除步骤①操作区的「一键载入示例资料」按钮（静态 HTML 与动态模板两处），操作区现仅保留「开始智能解析」。
- **同步清理**：删除 `loadSample` 事件分发分支；初始化提示语改为「请点击『新建编制』或上传资料，开始工作计划编制」。
- **功能保留**：`loadSample()` 函数仍被「新建编制」弹窗的「使用示例资料」流程引用，予以保留。
- **质量验证**：JS 语法通过、标签配平完整（div 254/254、button 53/53）、无残留；jsdom 冒烟 21/21 通过、0 报错。

## [v1.9.9] - 2026-09-07

### 工作计划编排智能体（plan.html）步骤①类型选择区后移

- **类型选择区位置调整**：应用户要求，「计划类型」下拉与详情面板移至已上传文件列表下方；卡片内顺序现为：上传附件/粘贴文本 → 文件列表 → 计划类型选择与详情 → 操作按钮。未上传文件时类型选择区直接跟随资料来源模块。
- **质量验证**：JS 语法通过、标签配平完整（div 254/254）；jsdom 实测有文件时顺序为 src-grid → filelist → ptype-row → ptype-detail；冒烟 20/20 通过、0 报错。

## [v1.9.8] - 2026-09-07

### 工作计划编排智能体（plan.html）粘贴模块简化与稳定性修复

- **「添加到资料」按钮删除**：应用户要求，移除粘贴文本模块右下角的「添加到资料」按钮；粘贴文本改为点击「开始智能解析」时自动纳入资料清单（解析前自动入库并清空输入框），功能闭环保持不变。
- **同步清理**：删除 `addPaste` 事件分支与 `addPasteText()` 函数，`syncPasteCount()` 仅保留字数统计职责；静态 HTML 与动态渲染模板两处按钮均已移除，无残留。
- **稳定性修复**：页面经设计工具再加工后顶栏撤销/重做/导出按钮（#mUndo/#mRedo/#btnExport）已不存在，但 `updateToolbar()` 仍直接引用导致加载报错；已改为存在性判空后再操作，消除 5 处 JS 报错。
- **质量验证**：JS 语法通过、标签配平完整（div 254/254）；jsdom 冒烟 20/20 通过、0 报错，覆盖粘贴文本随解析入库、四步流转与步骤④文稿预览。

## [v1.9.7] - 2026-09-07

### 工作计划编排智能体（plan.html）步骤①模块顺序调整

- **资料来源前置**：应用户要求，「上传附件 + 粘贴文本」双模块移至「计划类型」选择区上方；卡片内顺序现为：资料来源 → 计划类型选择与详情 → 文件列表 → 操作按钮。
- **质量验证**：JS 语法通过、标签配对完整（div 229/229）；jsdom 渲染确认 uploadBody 子元素顺序为 src-grid → ptype-row → ptype-detail → toolbar-inline；冒烟 21/21 通过、0 报错。

## [v1.9.6] - 2026-09-07

### 工作计划编排智能体（plan.html）类型说明文字移除

- **步骤①类型行精简**：应用户要求，移除计划类型下拉框右侧的类型说明文字（如「统筹全年工作，覆盖各领域目标任务与量化指标」）；类型行现仅保留「计划类型」标签与下拉选择框。
- **同步清理**：删除对应的 `PTYPE_DESC` 数据常量与 `.ptype-brief` 样式，无残留。
- **质量验证**：JS 语法通过、标签配对完整（div 229/229、span 109/109）；jsdom 冒烟 21/21 通过、0 报错。

## [v1.9.5] - 2026-09-07

### 工作计划编排智能体（plan.html）界面元素精简

- **步骤①类型详情面板精简**：应用户要求，移除「计划周期」展示项；类型详情面板现仅展示「编制单位」「成文模板」及章节结构。
- **步骤④成文信息条保留**：格式生成环节信息条中的「计划周期」属于成文元数据，未在本次选中范围内，予以保留。
- **质量验证**：JS 语法通过、标签配对完整（div 230/230、span 109/109）；jsdom 冒烟 21/21 通过、0 报错。

## [v1.9.4] - 2026-09-07

### 工作计划编排智能体（plan.html）右侧实时预览移除

- **预览整体移除**：应用户要求，编辑视图右侧「工作计划实时预览」整块不再展示（较 v1.9.3 的门控占位方案更彻底）；编辑区扩展为全宽单列布局，编制过程更聚焦。
- **步骤④预览保留**：格式生成环节的「正式文稿预览」面板属于成文工作台组成部分，予以保留，全屏预览入口不变。
- **质量验证**：JS 语法通过、标签配对完整（div 237/237）；jsdom 冒烟 25/25 通过、0 报错，覆盖预览移除、四步流转、下拉切换联动、粘贴入库及步骤④文稿预览。

## [v1.9.3] - 2026-09-07

### 工作计划编排智能体（plan.html）步骤①交互优化

- **实时预览门控**：右侧「工作计划实时预览」在步骤①期间不再提前渲染，显示"预览尚未开启"占位提示；仅当点击「进入思路与目标凝练」进入步骤②后才开始实时展示文稿，返回步骤①时自动回到占位状态。
- **计划类型改为下拉选择**：原三张类型卡片替换为下拉选择框（默认选中当前类型），下拉下方新增详情面板，展示所选计划的说明、计划周期、编制单位、成文模板及章节结构（与模板库一致），切换类型联动刷新；切换确认弹窗与示例资料同步载入逻辑保持不变。
- **粘贴文本模块**：步骤①资料来源改为「上传附件 + 粘贴文本」双模块并排布局；粘贴模块提供文本输入区、实时字数统计与「添加到资料」按钮（空内容禁用），添加后作为 .txt 资料进入清单并可参与智能解析。
- **质量验证**：JS 语法通过；关键标签配对完整（div 246/246）；jsdom 运行时冒烟 29/29 通过、0 报错，覆盖预览门控、下拉切换联动、粘贴入库与解析流转；原文件已备份为 `plan.bak.html`。

## [v1.9.2] - 2026-09-04

### 工作计划独立工作台标注完善

- **业务功能点标注**：依据工作计划智能体调研报告重建 `plan.html` 标注，共 20 个业务标注，覆盖三类编制场景、四步前置关系、资料处理、模板治理、版本审计、权限及异常处置。
- **全局原型说明**：新增产品目标、端到端流程、正式状态流转、角色权限、核心字段、异常边界、待确认事项及建议验收场景等 6 个栏目。
- **双模式评审**：支持浮动标注与三栏审阅，补齐页面、抽屉和通用弹窗作用域，不改变原有业务视觉和交互逻辑。
- **单文件交付**：标注样式、运行时及依赖均已内嵌，完成质量门禁、单文件审计和当前页面浏览器快速检查。

## [v1.9.1] - 2026-08-27

### 通用原型设计规范补充

- **规范预览升级**：将原阶段一 `design-system-preview.html` 升级为可供后续业务系统原型复用的完整组件手册，增加目录、版本、文件职责、组合模板和交付检查。
- **公共组件扩展**：补充文字链接、图标按钮、开关、单选/复选、校验信息、面包屑、页签、分段选择、步骤条、页面头、卡片、上传、进度、骨架屏、空状态、通知和分页样式。
- **Token 完善**：补齐 Figma 绿色/蓝色色阶、成功/警告/危险状态色、布局尺寸、弹框遮罩和悬停阴影等语义变量。
- **跨系统复用说明**：补充原生 HTML、Vue、React 接入方式、组件命名约定、管理页面结构和原型绘制流程。
- **规范映射更新**：同步更新 Figma 设计规范映射与组件样式规范；记录 Figma Starter 调用额度限制下仍待二次精确核验的细节项。
- **可分发规范包**：新增 `政务AI通用原型UI规范包-v2.0`，内含 Codex Skill、标准改造/验收/批量治理提示词、可视化组件手册、公共样式、审计脚本和拖入式 HTML 准备入口；默认创建副本，不覆盖同事原始原型。

## [v1.9.0] - 2026-08-26

### 模型能力管理第一阶段

- **菜单升级**：原「模型配置」更名为「模型能力管理」，统一维护对话生成与功能模型服务。
- **模型能力分类**：新增对话生成、语音转文字（ASR）、文档解析、OCR、Embedding、Rerank 六类模型能力。
- **接入信息扩展**：模型服务支持配置部署方式、协议类型、接口地址、提供商、使用范围和可选 API Key。
- **动态能力参数**：新增表单根据能力类型动态展示 ASR 音频参数、文档解析文件参数、OCR 版面参数、Embedding 向量参数及 Rerank 重排参数。
- **预置服务数据**：新增 FunASR、政务文档解析、PaddleOCR、BGE-M3 Embedding 和 BGE Rerank 示例服务。
- **模型清单调整**：列表将「模型服务」明确为「模型服务名称」，独立新增「模型名称」，删除「业务引用」，运行状态改为可操作开关，并支持按能力类型、部署方式和健康状态筛选。
- **新增表单重排**：模型服务按「能力类型 → 提供商/框架 → 模型服务名称 → 模型名称」顺序录入；对话生成模型的能力参数不再展示上下文长度。
- **分级权限完善**：普通使用人员仅可查看可用对话模型并维护个人对话模型，不展示语音识别、文档解析、OCR、Embedding、Rerank 等功能模型；委办局管理员维护个人/部门对话模型，对功能模型仅可查看。系统管理员维护个人/公共对话模型和平台公共功能模型，超级管理员支持跨租户治理。凭证统一不回显明文，按角色限制查看和更新范围。
- **分类能力测试**：对话模型保留对话测试，功能模型按类型提供音频、文档、扫描件、向量和重排测试入口及结果反馈。
- **聊天模型隔离**：聊天框模型选择器仅读取启用且有权限的对话生成模型，不展示 ASR、OCR、文档解析、Embedding 和 Rerank 服务。

## [v1.8.0] - 2026-08-25

### 业务连接器管理页面重构

- **页面结构对齐**：参考业务连接器管理页面，重构连接器总数、启用数、停用数统计区，以及连接器列表、筛选条件和操作区。
- **管理信息完善**：连接器列表补充描述、所属部门、更新时间、创建时间、运行状态和连接状态，继续保留平台现有连接器数据。
- **连接器生命周期操作**：完善查看详情、配置部门、测试连接、同步工具、新增操作、启用与停用等管理入口。
- **新增 MCP 连接器表单**：按照最新页面补充连接器图标、名称、描述、业务分类、MCP 接入方式、SSE / Streamable HTTP、MCP 服务地址、认证方式、超时时间、运行状态及作用部门配置。
- **认证条件联动**：支持无需认证、API Key、Bearer Token；Bearer Token 模式下展示认证 Header 与 Token，API Key 模式仅展示 API Key。
- **部门多选体验**：作用业务部门改为卡片式多选，支持全选当前一级部门并实时显示已选数量。
- **新增流程调整**：连接器技术 ID 改为自动生成，新增时不再要求配置首个业务操作；保存并开启后再测试连接、同步工具和添加业务操作。
- **添加业务操作弹窗调整**：`添加操作` 改为从测连后 `tools/list` 入库结果选择 MCP 工具名，调用方式固定为 `MCP tools/call`，并补充操作名称、操作类型和用户问题参数名配置；写工具在当前阶段展示但不可选择。
- **工具与操作解耦**：同步工具不再从已配置业务操作反向生成工具清单，新增操作会记录 MCP `tool name`、参数映射和调用方式，并校验同一工具不可重复添加。
- **筛选与刷新**：支持按连接器名称、所属部门、运行状态筛选，并提供列表刷新与一键重置。
- **响应式适配**：优化桌面端和移动端布局；移动端统计卡片与数据表支持横向浏览，避免信息被过度压缩。
- **原型验证**：完成桌面端、移动端及关键弹窗/操作验证，浏览器控制台无报错。

## [v1.7.0] - 2026-08-24
### 模型配置(修改)

- **模型管理列表**：
新增「启用状态」「模型展示名称」「连接状态」列，新增「查看」按钮（弹窗查看模型详情），新增「测试连接」所有角色可见、可用；
- **模型配置**
高级配置删除「工具调用」项，新增「模型展示名称」仅保留「图片输入」「自定义协议」
- **权限规则**
普通使用人员仅可创建个人模型，委办局管理员可创建个人或部门模型，系统管理员可创建个人或公共模型。普通使用人员可管理个人的模型，部门和公共的只能查看，委办局管理员可管理个人和部门的模型，公共的只能查看，系统管理员可管理个人、部门、公共模型；个人的模板只有自己才能查看和使用；

### 租户管理(新增) 

新增“超级管理员”，租户管理仅该身份可见。
租户清单支持筛选、新建、查看、编辑、启用和停用。
支持为租户挂载多个组织、配置多名租户管理员。
支持授权业务系统。

## [v1.6.0] - 2026-08-21

### 智能体与业务连接器治理

- **智能体接入配置完善**：智能体管理新增图标、名称、描述、智能体 URL、`agentSn`、JSON 输入参数、智能体类型、使用方式、作用业务部门和启用状态配置；支持新增、编辑、删除、连接测试和接入状态展示。
- **治理与广场联动**：智能体启停、作用部门、名称、描述、图标和接入地址调整后同步更新智能体广场；未授权部门不可见、不可调用，历史对话不受智能体删除影响。
- **连接器多操作管理**：业务连接器支持在一个连接器下维护多个查询、新增、更新、提交等业务操作，配置请求方法、接口路径、输入参数、输出映射、启用状态及是否需要确认。
- **外部连接器接入**：连接器治理支持中台同步与手动 HTTP API 接入，维护基础地址、认证方式、业务分类、作用部门、连接状态和运行状态，并支持连接测试。
- **读写操作分级执行**：查询类操作可直接返回授权范围内结果；创建、更新、提交、发送等写操作执行前展示连接器、业务操作、调用身份和提交内容，由用户二次确认后执行。
- **操作审计补充**：智能体和连接器的新增、编辑、启停、删除、连接测试、部门范围调整及业务调用统一记录操作日志。

### 智能体广场与连接器广场

- **智能体详情重构**：点击智能体卡片可查看智能体类型、使用方式、调用次数、发布时间和模板信息；「立即使用」按会话型或独立工作台自动进入对应处理场景。
- **模板选择与默认模板**：详情弹框展示当前使用模板，支持预览、切换、设为默认模板及进入模板管理；模板选择结果与后续会话生成保持一致。
- **部门可见性过滤**：智能体和连接器广场按当前角色及作用业务部门过滤可见范围，治理端停用后同步从可用列表中移除。
- **连接器操作前置选择**：从首页、资源选择器或连接器广场使用连接器时，先选择已启用的具体业务操作，再将对应示例需求带入对话框。

### 核查规则管理

- **新增「核查规则管理」模块**：配置治理中心新增政务行文、政策法规、制度和保密四类规则库；普通使用人员无管理权限，系统管理员和委办局管理员按数据范围管理。
- **规则库管理**：支持新增、查看、编辑、删除规则库，维护规则库编码、描述、来源、作用委办局、作用角色和启用状态；部门管理员的维护范围不得超出本部门。
- **规则管理**：支持按规则名称、适用文种、依据层级、作用角色和状态筛选；可维护命中条件、问题提示、修改建议、依据文件、依据条款、官方来源和作用范围，并支持启用、禁用和删除。
- **预置核查数据**：内置 4 个规则库、25 条示例规则，覆盖国家级、江苏省、南京市和江北新区的公文格式、规范性文件程序、数据共享开放、政策解读、个人信息与保密等核查场景。
- **数据库导入表**：新增《核查规则数据库导入表.xlsx》，包含「核查规则导入」「规则库导入」「字段说明」三个工作表，并同步输出 CSV、字段预览和规则预览，便于研发建立规则库表结构和批量导入。

### 五个独立工作台优化

- **文本智能润色**：主流程收敛为「载入文稿—智能润色—确认并导出」三步；空态支持直接上传或粘贴正文，自动识别文种并保护数字、日期、机构、人名和政策名称；采纳或忽略后自动定位下一条，高风险建议批量采纳前必须人工确认。
- **会议纪要智能生成**：会议名称改为可选并支持自动生成；纪要模板由系统自动匹配并可查看；结果页优先展示议题，并增加明确结论、待办和需要人工补充事项的摘要提示。
- **合同智能生成及预审**：经办人流程收敛为「填写信息—生成草案—预审修订—导出提交」四步；明确模板、条款和规则由法务治理中心维护；风险问题展示规则来源，高风险建议必须逐条确认，忽略时需填写原因并记录操作留痕。
- **PPT 智能生成**：默认流程收敛为「材料与目标—大纲生成—预览修改—检查导出」四步，高级设置按需展开；标题和副标题可根据主题、用途和场景自动生成；终检增加逐页来源覆盖、数字事实冲突、文本溢出、字体版式及政务表述一致性检查。
- **政务文件解读**：新增章、条、款、项层级解析和来源位置标识；适用对象、申报材料、办理主体、时限和限制条件均回链原文；原文未明确的结论统一标记「原文未明确」，不再由系统推断补齐。

### 分析与交付材料

- 新增五个独立工作台走查总览，以及文本润色、会议纪要、合同、PPT、政务文件解读五份专项分析报告。
- 新增核查规则数据库构建脚本、Excel/CSV 导入文件、字段说明和可视化预览产物。

---


## [v1.5.0] - 2026-08-19 ~ 2026-08-20

### 发言稿讲话人智能识别

- **按用户指令优先匹配讲话人**：发言稿生成时自动识别用户输入中的讲话人姓名；用户明确指定讲话人后，优先从当前用户的讲话人风格库中匹配，不再继续使用模板中预设的其他讲话人风格。
- **生成过程轻提示**：识别成功后，在生成状态中展示“已识别讲话人：XX　使用XX讲话风格”，不新增主页选择控件，也不通过弹窗打断正常生成。
- **多结果选择**：存在多个同名讲话人时才弹出选择框，列表展示讲话人姓名及职务或适用场景，由用户确认本次使用对象。
- **未匹配兜底**：未找到对应讲话人风格时明确提示，不静默退回模板中的其他讲话人；用户可选择“使用默认政务风格”或“选择其他讲话人”。
- **结果信息收敛**：生成结果不再重复展示“使用模板、讲话人风格、风格来源”等元数据，保持正文结果简洁。

### 讲话人风格管理

- **列表字段完善**：讲话人列表统一展示讲话人姓名、职务或适用场景、已学习材料数量、学习状态、最近学习时间及操作。
- **状态展示优化**：学习状态统一为“已就绪”或“正在学习中”，未学习数据使用明确的空状态提示。
- **操作统一**：每条讲话人记录提供查看、编辑、删除操作；讲话人风格数据继续仅对当前用户生效。

### 智能体模板管理

- **当前模板标识**：同一智能体存在多个模板时，为当前默认模板增加“当前使用”标签，便于用户快速识别实际生效模板。
- **支持切换当前模板**：非当前模板新增“设为当前使用模板”按钮，点击后立即切换默认模板，并与智能体广场的模板选择结果保持一致。
- **单模板自动生效**：智能体只有一个可用模板时，自动将该模板作为当前使用模板，无需用户再次选择。
- **权限菜单同步**：菜单管理中的智能体模板管理按钮范围同步增加“设为当前使用模板”。

### 个性化

- **取消默认人设**：“人设/人格描述”首次进入保持为空，不再根据用户当前角色或所属部门自动生成默认人设，避免多角色用户被套用单一角色设定。
- **删除恢复入口**：移除“恢复角色默认值”按钮、相关重置逻辑及菜单权限项。
- **仅使用主动配置**：只有用户主动填写并保存的人设才会应用于后续对话；人设为空时不附加预设人格，角色切换也不会自动改变已保存内容。
- **清空状态明确**：用户清空内容并保存后保持无人设状态，并提示“人设/人格描述已清空”。

### 质量检查

- 完成内联 JavaScript 语法检查，以及讲话人匹配分支、当前模板切换、个性化空白默认状态和旧重置入口清理等静态检查。

---

## [v1.4.0] - 2026-08-18

### 词库管理（配置治理中心新模块）

- **新增「词库管理」菜单**：系统管理员与委办局管理员可见，普通用户无访问权限；系统管理员可管理全部词库，委办局管理员仅可管理作用范围包含本部门的词库。
- **左树右表布局**：左侧为词库面板（搜索 + 新建词库 + 词库列表），点击词库后右侧展示该词库下的词条列表（词语名称、检测范围、处理方式、建议替换内容、启用状态、操作）。
- **词库操作**：词库项「···」弹出菜单支持作用范围配置（树状组织机构多选，全选即「全部委办局」）、编辑（名称/类型/描述/启用状态）、详情、删除；词库类型含敏感词、负面情绪词、不规范表述、其他四类。
- **词条管理**：支持查看、编辑、启用/禁用、删除；处理方式仅「禁止提交」和「自动替换」两种，自动替换强制填写建议替换内容；检测范围支持用户输入、模型输出、全部。
- **批量导入**：词条列表「导入」按钮弹出导入弹框，支持下载 xlsx 模板、拖拽/点击上传文件（xlsx/xls/csv/txt，单文件上限 10000 行），自动校验枚举值、去重已有词条，导入结果计数提示并记录审计日志。
- **导出**：「导出」按钮将当前词库全部词条导出为 Excel（词语名称/检测范围/处理方式/建议替换内容/启用状态），记录审计日志。
- **命中记录**：保留命中记录 Tab，展示命中词语、所属词库、检测范围、处理方式、命中内容、用户、时间，支持条件筛选。

### 讲话人风格管理

- **列表页 + 弹框模式重构**：由卡片选择改为列表页（名称/职务/参考材料数/学习状态/操作），新增、编辑、查看、删除统一走弹框。
- **弹框内容**：基本信息（名称、职务）、学习风格约束词（默认 4 项可增减）、参考材料（上传/查看/删除/开始学习）、讲话人风格内容（Markdown 编辑），学习过程带状态动画。
- **更换风格弹框内容对齐**：发言稿「更换讲话人风格」弹框中，每个讲话人卡片直接展示统一格式的风格内容（讲话人姓名/职务 + 词汇偏好/句式结构/修辞习惯/表达特色四行），与配置数据保持一致，不再展示五维标签摘要。
- **管理入口前置**：弹框底部操作栏新增「管理讲话人风格」按钮（位于「切换并更新发言稿」左侧），点击后直达配置中心 → 模板治理 → 讲话人风格管理页。

---

## [v1.3.0] - 2026-08-17

### 用户与组织管理

- **组织树联动用户清单**：用户管理改为左侧组织机构树、右侧用户清单布局；系统管理员可查看全部授权组织，委办局管理员仅能查看本委办局，点击组织节点后联动展示该机构下的用户。
- **用户多角色管理**：同一用户支持同时配置多个角色，列表以角色标签展示，角色筛选同步支持多角色数据。
- **用户维护边界收敛**：取消新增用户；用户账号、姓名和所属组织机构由统一身份系统同步并保持只读，操作列仅保留「编辑」，编辑时只能修改所属角色和账号启用/禁用状态。
- **组织信息精简**：组织机构树不再展示「委办局」类型和「启用」状态，只保留组织名称及层级关系。
- **权限菜单同步**：菜单管理中的用户管理页面按钮同步调整为「编辑」。

### 配置治理中心

- **筛选条件拆分**：智能体、业务连接器、智能体模板、用户、角色、操作日志和模型配置中的复合搜索框拆分为独立字段筛选，支持按名称、类型、使用方式、组织、角色、状态、创建人、日期等条件分别查询。
- **数据范围保持一致**：各筛选条件继续受当前登录角色的数据权限约束，委办局管理员无法通过筛选查看本部门以外的用户和操作日志。

### 智能体模板管理

- **Markdown 只读化**：删除「导入 Markdown」「导出」「在线编辑」按钮，Markdown 模板暂时仅提供只读预览，结构化参数仍可正常维护。
- **模板内容精简**：删除 Markdown 中「平台基础约束」章节及“平台安全、保密、权限和审计基线由智能体中台统一下发，本页面不维护”说明。
- **列表信息优化**：模板列表不再展示默认生成的“四段式文稿大纲”；调用次数、创建时间和更新时间调整为同一行展示，并压缩卡片高度、优化操作区布局。

### 首页与新对话

- **默认模型文案**：模型自动路由选项由「Auto」调整为「默认模型」。
- **首屏视觉精简**：删除主页和新对话页面对话框上方的江北新区 N 字图标，保留欢迎标题与核心输入区域。
- **快捷模板居中**：主页和新对话的快捷模板按钮改为居中、自适应换行展示，在不同数量和屏幕宽度下保持视觉平衡。

### 质量检查

- 完成内联脚本语法检查，以及用户编辑边界、组织树联动、字段级筛选、模板只读化和首页布局等静态结构检查。

---


## [v1.2.0] - 2026-08-14

### 知识库

- **视图页签收敛**：知识库页签由「个人 / 通用 / 政策 / 数据局内部 / 我的成果」五个收敛为「个人知识库 / 组织知识库 / 我的成果」三个，默认选中组织知识库。
- **组织知识库两级树**：左侧按「通用知识库 / 政策知识库 / 数据局内部知识库」三个子库分组，每组下展示文件夹；点击文件夹，右侧列表展示该文件夹下知识库文件。
- **选择器结构对齐**：首页「知识库」按钮与新对话框「@」资源菜单的知识库选择，统一改为「个人知识库 / 组织知识库」两级入口；选「组织知识库」后逐级「子库 → 文件夹 → 文件」选择，与视图结构一致，支持引用整库与引用具体文件，面包屑逐级返回。

### 主平台对话与模型

- **回复耗时反馈**：主页和新对话的 AI 回复元信息由「生成时间」调整为「已完成 + 本次实际耗时」，便于用户判断任务执行时长。
- **失败任务续做**：模型执行失败时展示专用失败提示和重试确认；确认后自动发送「请继续完成未完成的任务」，沿用上一次任务上下文继续执行。
- **语音输入**：主页和新对话增加语音输入，支持录制计时、取消、完成、转写中状态及在当前光标位置插入转写文字；录音条展示在发送按钮旁，录音期间隐藏模型选择和语音入口。
- **语音交互样式**：语音按钮使用独立 SVG 图标，默认无边框和底色、悬停时显示背景；录音取消和完成统一为圆形点击区域。
- **模型选择**：主页和新对话支持切换内置及自定义模型；Auto 模式使用独立 SVG 图标，模型列表改为紧贴模型文字上方的小型浮层。
- **自定义模型配置**：配置治理中心新增模型配置页，支持新增、编辑、删除模型，维护提供商、接口地址、必填 API Key、模型参数值、工具调用、图片输入、自定义协议及输入/输出设置。
- **模型权限范围**：普通使用人员仅可创建个人模型；委办局管理员可创建个人或部门模型；系统管理员可创建个人或公共模型，模型列表按当前用户、部门和公共范围过滤。

### 智能体广场与模板

- **模板入口分流**：对话型智能体只有一个模板时展示「配置更多模板」，直接进入该智能体的新增模板页面；存在多个模板时展示模板选择弹层并提供「管理模板」入口。
- **模板内容预览**：模板选择列表支持逐项展示 Markdown 内容预览，便于用户在选择前确认模板结构和约束。
- **模板分级发布**：普通使用人员创建的个人模板可发布到部门，委办局管理员可将部门模板继续发布到公共范围，并支持逐级撤回。
- **讲话人解耦**：发言稿模板不再绑定讲话人风格，生成时统一读取当前登录用户的讲话人配置。

### 配置治理与权限

- **角色可见范围**：委办局管理员仅查看并管理本委办局用户；普通使用人员仅保留个性化和权限范围内的智能体模板管理；系统管理员保留完整治理能力。
- **操作日志**：原「部门操作记录」统一为「操作日志」，记录登录登出、智能体、连接器、知识库、模板和配置操作；委办局管理员仅查看本部门人员日志。
- **角色管理**：支持新建角色；「使用菜单范围」改为与菜单管理一致的三级选择树，可精确勾选一级菜单、二级菜单和页面按钮，并支持父子联动及回显。
- **菜单管理**：按实际页面按钮重建权限项，采用「一级菜单 → 二级菜单 → 页面按钮」三级树结构；一级菜单、二级菜单和按钮均支持重命名、删除，一级菜单可新增二级菜单或按钮，二级菜单可新增按钮。
- **治理规则收敛**：删除「本部门强制约束规则」模块，部门配置统一继承平台基础约束。

### 讲话人风格

- **当前用户绑定**：讲话人风格改为当前用户级配置，不再随发言稿模板保存；不同用户分别维护默认讲话人和个人风格数据。
- **参考材料管理**：已上传材料支持查看和删除；新增「开始学习」，学习过程中展示状态并可停止，完成后输出可在线编辑的 Markdown 学习结果。
- **自定义风格约束**：取消词汇偏好、句式结构、修辞习惯、表达特色四项固定约束，改为可自由输入和修改的多行风格规则。

### 对话文件与在线编辑

- **下载格式选择**：对话生成文件和右侧编辑器的「下载」均支持 Word、PDF、Markdown 三种格式；对话文件名称不再直接展示 `.docx` 后缀。
- **分享链接**：右侧编辑器新增「分享」，复制内容统一为「【江北新区 AI 文档】+ 文档名称 + 文档链接」，并提示「已复制分享链接」。
- **编辑器入口调整**：隐藏「标准版式预览」；保留分享、复制、下载和历史记录等常用操作。
- **回复操作收敛**：普通 AI 回复移除「重试」按钮，仅失败状态保留续做入口；「AI 生成可能有误 注意核实」取消背景色，改为普通提示文字。

### 智能体工作台改版（decision / report / notice / speech / summary）

- **report（报告请示）整页重写为 Vue 3 + Naive UI 单文件应用**：新增三层权限（超管 / 部门管理员 / 普通用户）驱动审核归档、规则豁免、数据隔离与发文单位锁定；新增发文字号 / 签发人 / 紧急程度 / 保密期限 / 抄送等公文格式要素；办文目的按文种差异化（报告 3 类 / 请示 4 类，共 7 子场景）生成差异化正文与提纲；新增行文提纲预览、正文 source-mark 溯源弹窗、审核 / 归档闭环（草稿→提交→审核→归档 / 退回）、审计日志视图；校验拆分为文种属性与格式两类，支持采纳 / 撤回并新增发文字号、签发人阻断规则。
- **decision / notice / speech / summary 四页统一改版**：视觉换肤为 AI 蓝（#0E4C92）+ 渐变顶栏，整体改 CSS Grid 布局、预览与输入面板左右互换；新增 AI 动效规范（打字光标、思考点、渐入、渐变标题）及 `prefers-reduced-motion` 适配；弹窗 / 下拉 / Toast 统一进出动画与毛玻璃，卡片悬停上浮；移除原型版本号注入。
- **decision（决定决议）**：生成流程由六步精简为三步（材料录入 → 公文生成 → 输出交付），删除文种模板库 / 程序规则库 / 生成设置 / 历史台账 / 统计报表五个视图；新增深色玻璃拟态「AI 智能体工作条」随步骤联动；正文下方新增「知识来源 / RAG 可溯源」卡片；合规校验降级为参考性、不再阻断；决策主体 / 事项类型改 input+datalist 可手输，文种选择前置，历史记录移入可折叠侧边栏并支持恢复草稿。
- **notice（通知通告）**：移除类型识别「准确率」展示与预览折叠 / 聚焦态（仅保留 initial / split 两态）；生成遮罩改为 transition 淡出回调，内容在生成完成后才渲染。
- **speech（讲话稿）**：讲话人库扩展（语料篇数 / 场景数 / 版本 / 置信度 / 更新时间）；「风格提示词」改为部门已发布五维风格画像（场景策略 / 受众策略 / 结构逻辑 / 语言表达 / 语气节奏，只读）；「政策依据」整体更名为「生成依据」（本次任务要素 / 上传资料 / 风格档案 / 缺失事实占位）；会议场合、听众类型由必填改为可自动判断，时长默认 10 分钟；初始化改为新建空白草稿。
- **summary（工作总结）**：取消大纲阶段，改为一步流式生成总结正文（「智能生成大纲」改为「智能生成总结」）；预览区移除「大纲 / 正文」双 Tab 改为单一视图；溯源高亮由黄色改为蓝色，未归类提示由 warning 改为 primary。

### 文档与素材

- 新增研发交付版 PRD：《南京江北新区政务AI办公助手_PRD_研发交付版.md》
- 新增全平台业务流程泳道图（drawio 源文件 + png + 蓝图 md）及构建脚本 `scripts/build_business_swimlane.js`；新增系统架构图（png）
- 新增江北 logo 素材 `assets/jiangbei-mark.png`
- 新增《功能清单.xlsx》《智能体提问字段汇总表.xlsx》《第一批次起草类智能体-模板样式收集说明.docx》
- 新增《公共能力集市周报-2026W32(8.4-8.6).md》
- 新增「17 个智能体调研准备材料」目录（17 个智能体分册）

---

## [v1.1.0] - 2026-08-06

### ✨ 新增

- 主页「常用推荐」：个性化页可配置 5 个常用智能体，主页对话框下方按配置顺序展示，末尾「更多」跳转智能体广场
- 对话「重试」按钮：AI 回复支持重试，断线/中断后回到用户提问重新生成

### 🐛 修复

- 生成文件「下载」按钮无法下载（事件冒泡被阻止，委托分发器收不到点击）
- 主页右侧任务流浮框消失（CSS 覆写误隐藏 `#homeRail`）
- 时间显示缺少秒数：「修改于」与回复时间统一为「年-月-日 时:分:秒」

### ♻️ 重构 / 体验优化

- 生成文件卡与提示文字合并到同一条消息气泡（附件式，一个问题对应一个答案）
- 直生成「思考过程」与「回答」合并到同一条气泡
- 不再自动打开右侧编辑器，改为点击文件卡才展开
- 「关键信息确认」改为普通文字样式，与正常回复一致
- 「复制」按钮只复制回复文字，不再包含文件正文
- 右侧编辑器布局重构：顶部工具栏 + 大纲 + 编辑区 + 满宽底栏

### 🎨 样式

- 文件卡：悬停显示「下载」按钮，移除右侧箭头与「v3.0 先出稿后迭代」角标，撑满气泡宽度
- 编辑器：移除文档标签「关于XXX的报告 ×」
- AI 回复头像改为江北 logo
- 「AI 生成可能有误 注意核实」去掉黄色背景，改为普通文字
- 移除普通回复里「当前需求：xxx」模块
- 任务流卡片不显示「当前节点·4/5·合规校验」
- 删除主页对话框「@ 选择智能体」「选择连接器」按钮

### 📝 文档

- 新增研发向 README，门户入口 `南京江北新区政务AI智能体大平台.html` 重命名为 `index.html`

## [v1.0.0] - 2026-08-06

### 🐛 修复

- release.mjs 改为逐条目拷贝，避免 cpSync 拷进自身子目录 (fe5051a)
- stamp.mjs 被 import 时跳过 CLI 入口（isMain 守卫） (ef007c4)

### 🔧 杂项

- 初始化工程化原型项目（纳入版本管理） (f5f7229)



<!-- 新发版条目由 `npm run release` 自动插入此处 -->
