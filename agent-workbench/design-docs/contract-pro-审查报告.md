# contract-pro.html 质量审查报告（Phase 4）

> 审查人：严过审（critique-reviewer）· 审查对象：`agent-workbench/contract-pro.html`（3054 行）· 审查依据：`contract-需求摘要.md`（15 项差距矩阵）+ `contract-DESIGN.md`（§1.2 令牌 / §二 7 组件 / §三 复用映射 / §四 Anti-Slop 8 红线）
>
> 验证方式：静态通读全文 + jsdom 独立冒烟（63 项断言，最终 61 通过 / 0 JS 错误；2 项初判失败经复核修正为「非阻断」）。

---

## 一、结论：**通过**（第 1 轮修订复审后更新；原结论为「有条件通过」，6 项 P1 已全部关闭，详见 §七）

P0 = 0。第 1 轮修订后 jsdom 复审 17 项断言 16 过、0 JS 错误（唯一 FAIL 为本审查断言口径过严，非构建缺陷，见 §七 P1-3）。

---

## 二、5 维评分表（10 分制）

| 维度 | 得分 | 评语 |
|------|:----:|------|
| 需求覆盖度 | **9** | 15 项全部有对应视图/交互落点；唯一缺口是旅程 2「疑点穿透到法规库原文」为 toast 级演示（`chainLaw` 仅 switchView+提示），及穿透查询仅行内展开、未复用新抽屉 |
| 令牌一致性 | **9** | §1.2 全部 24 个新令牌已追加至 `:root`（36–58 行）；新视图内联 hex 仅 5 处且全部为既有「补充常用中性色」白名单内值，无规范外色 |
| 组件规范符合度 | **9** | 7 个新组件（nav-tree / kg-panel / mc-layout / cmp-layout / job-steps / chain-card / lv-tag）骨架与 CSS 均按 §二 落地；`.drawer` 为 §三 唯一允许新增容器，已遵；作业步骤条正确复用 `.steps` 仅加 `.archived` 变体 |
| Anti-Slop 门控 | **8** | 8 条红线无硬违反（无花哨渐变、无破碎布局、对比度达标、双编码齐）；扣分项：新视图出现 4 个 ⬆/⬇ 符号图标（红线 1 灰色地带）、模型画布/比对图占位区缺「演示数据」标注 |
| 交互完整性与健壮性 | **8** | jsdom 63 断言 61 过、0 JS 错误；13 视图切换/导航同步/抽屉/图谱/Tab/画布/穿透/归档流转全部正常；2 个 P1 缺陷（台账首进不渲染、drawerCloseBtn 未接线）扣分 |

**加权结论：全部维度 ≥ 8，无 < 8 维度。**

---

## 三、问题清单

### P0（阻断）—— 无

### P1（应修，6 项）

| # | 问题 | 位置 | 修复建议 |
|---|------|------|---------|
| 1 | **历史台账首次进入空白**：`switchView("history")` 未调用 `renderHistory()`，左侧导航直达「历史与版本」时表格 0 行（hCount 仍显示 8，自相矛盾）。用户从工作台以外入口进入必现 | `contract-pro.html:1703-1721` switchView | `switchView` 内补一行：`if (v === "history") renderHistory();` |
| 2 | **抽屉底部「关闭」按钮死链**：`data-action="drawerCloseBtn"` 在全局委托 switch 中无 case，用户点击无任何反馈（已用 jsdom 复现：点击后 drawer/mask 均保持 show）。唯一出路是 × 按钮与遮罩，与视觉承诺不符 | `2828-2829` 生成按钮；`2297-2694` 委托无 case | 委托内补 `case "drawerCloseBtn": closeDrawer(); break;` |
| 3 | **新视图出现 emoji 域符号 ⬆ / ⬇**：工具箱 3 处 `.up-ic`（1129、1221、1244 行）与项目库「⬇ Excel 导出」（1088 行）。DESIGN §四-1 允许「文字符号 ✓ ✕ › ▍ ▣」，⬆⬇ 属 U+2B00 符号区而非文字符号，且本文件自身合规实现（`.cmp-placeholder` 内联 SVG 图片图标）已证明可替代 | 1088、1129、1221、1244 行 | `.up-ic` 改为内联 SVG 上传图标（`M12 16V4 M6 10l6-6 6 6` + 底线），导出按钮用 SVG 下载图标或直接去掉符号 |
| 4 | **演示数据标注不全**：图谱（1031 行）、比对器（1185、1199 行）、抽屉疑点清单（2819 行）已标，但 **预审模型配置画布**（971-977 行静态节点+连线）与 **比对图 A/B 占位区** 未标。红线 8 要求「演示级交互的诚实表达」 | view-model 画布区、tbCompare 图片区 | 画布参数面板既有提示下补一句「画布连线为静态演示」；比对器 figcaption 后补 `<span style="font-size:11px;color:var(--muted);">演示数据</span>` |
| 5 | **modal 确认后遮罩残留**：`basisApprove` 等流程 `closeModal()` 依赖 `animationend`（modalOut）事件移除 `.show`；在动画被裁减（prefers-reduced-motion 全局 `animation:none`）或异常环境下 `.modal-mask.show` 会永久残留，阻断后续操作。jsdom 实测确认弹窗后 `modalMask.show === true` | `1402-1408` closeModal | closeModal 增加兜底：`setTimeout(()=>{ modal.classList.remove("closing"); modalMask.classList.remove("show"); }, 260);`（animationend 先到则无害） |
| 6 | **权限分级管控表达偏弱**：需求 1 要求「权限矩阵演示」级表达，现仅 page-sub 一句「当前角色：法务专员 · 可编辑/下架」+ 编辑器 `.role-scope` 提示条 | view-templates 825-828 行 | 低成本补强：page-head 加一枚 `tag low`「角色：法务专员」+ `tplTrace` 留痕弹窗已含操作人列，可在 page-sub 追加「权限：经办人只读 / 法务可编辑（演示）」 |

### P2（建议，5 项）

1. **依据/项目筛选按钮为演示级 toast**：`basisFilter`（2597 行）、`projFilter`（2608 行）只弹提示不真过滤——法规库/作业库筛选是真过滤，此处行为不一致，建议补真实过滤或至少在 toast 注明「演示」。
2. **统计图表硬编码亮色**：`renderStats` 中 `#2455e6 / #e0342a / #0b8fa8 / #d07a00 / #b83fce`（1670-1696 行）非令牌色且与政务蓝系偏离（既有视图遗留问题，非本次新增；如修订可顺手映射为 var(--primary)/var(--red)/var(--cyan)/var(--orange)/var(--purple)）。
3. **AI 工具管理为表格而非需求建议的「卡片式」**：需求 12 建议卡片式展示模型资产，实现为三分类表格——信息密度更高、可接受，但与需求描述有出入，可在报告留档说明。
4. **模板库无「历史回溯时间线」视图**：需求 1 增强点之三，`tplVersion` 弹窗仅有版本表无时间线可视化，留痕弹窗（tplTrace）承担了大部分语义，可视为达成。
5. **`renderLibTable` 猴子补丁**（3016-3031 行）：通过包裹原函数注入「生效/作废」按钮，功能正常但脆弱（依赖 cells[4] 文本匹配）；建议改为在 `renderLibTable` 内显式生成。另含 `const anchor` 死代码（1914 行）。

---

## 四、需求覆盖核对表（15 项逐项）

### 生成模块（6 项）

| # | 需求 | 结论 | 证据 |
|---|------|:----:|------|
| 1 | 模板库全生命周期（增强） | ✅ | 生效/作废流转：`tplFlow` 2468-2476 + 按钮注入 3016-3031（jsdom 验证流转反转成功）；变更留痕：`tplTrace` 2477-2485（4 条留痕记录）；权限表达：page-sub + role-scope（P1-6 偏弱） |
| 2 | 条款库管理 | ✅ | view-clauses + LIBS.clause 五分类、必备性标记、上传切分演示、编辑/删除留痕提示 |
| 3 | 智能要素填充与条款补全 | ✅ | 三步向导完整（jsdom 走通：场景卡→要素校验→匹配动画→生成）；步骤 3 含「补齐必备条款/剔除冗余」parse-line |
| 4 | 合规智能校验（四维逐句） | ✅ | 预审面板 9 问题卡（7 定位 + 2 结构）、四维筛选、原文/建议、高风险二次确认、来源标注 |
| 5 | 多版本管理与留痕 | ✅ | pushVersion/版本弹窗/diff/回溯还原全链路（既有，未回归） |
| 6 | 多格式导出 | ✅ | openDocReportModal 双 Tab + Word/PDF + 水印选项 |

### 预审模块（9 项，全部新建）

| # | 需求 | 结论 | 证据 |
|---|------|:----:|------|
| 7 | 法律法规库 | ✅ | view-law：4 区域分类 + 效力等级筛选 + **知识图谱**（7 节点/7 边，点击高亮+dim 弱化+空白复位，jsdom 验证通过）+ 新增法规弹窗 + `lawDiff` 版本对比 + `lawRevise` 审批流步骤条 |
| 8 | 预审依据管理 | ✅ | view-basis：lv-tag 四级效力 + 双来源（人工/AI）+ 审核入库流转（jsdom 验证 4→5）+ AI 自动匹配演示 |
| 9 | 预审模型配置 | ✅ | view-model：5 组件面板点击追加（3→4 验证通过）+ 画布节点 active + 参数面板联动（3 参数项）+ 3 标准模板卡 + 发布/保存 |
| 10 | 预审项目库 | ✅ | view-project：来源标记 tag + 基础查询 + 全文检索 checkbox + **穿透查询行内展开**（展开/收起验证通过）+ Excel 导出 |
| 11 | 预审作业与报告 | ✅ | view-jobs：4 状态 KPI + 作业列表 + **详情抽屉**（job-steps 流转条 + chain-card 依据链 ×2 + 报告生成 + 归档流转，全链路 jsdom 验证通过） |
| 12 | AI 工具管理 | ✅ | view-aitools：文本处理/OCR/预审专用三类 7 模型 + 版本/状态/调用量 + 启停切换验证通过（已下线保护逻辑存在） |
| 13 | 多模态导入 | ✅ | 工具箱 Tab1：上传区→预处理进度（4 阶段）→字段配置→智能提取→结构化结果表，全流程验证通过 |
| 14 | 图片比对工具 | ✅ | 工具箱 Tab2：双图并排 + 4 个差异红框（①②序号角标双编码）+ 相似度环（87 分 + 文字「较高」）+ EXIF 校验表 5 行 |
| 15 | 合同和图价识别 | ✅ | 工具箱 Tab3：合同识别 5 字段 + 人工校验编辑弹窗 + Excel 导出 + 图纸识别工程量/造价明细表，验证通过 |

**覆盖率：15/15 视图与关键交互落地；其中 3 项（旅程 2 穿透、权限矩阵、筛选真实化）为演示级深度，记 P1/P2。**

---

## 五、Anti-Slop 门控检查表（§四 8 红线）

| # | 红线 | 结果 | 说明 |
|---|------|:----:|------|
| 1 | 新视图禁 emoji | ⚠️ 边缘 | 新视图无真 emoji（🏢 系仅在既有 LIBS 数据与新场景无关的既有区）；但 ⬆×3 / ⬇×2 属符号图标灰色地带 → **P1-3**。✦ ★ → ① ② ▣ ✓ 均在允许文字符号范围 |
| 2 | 渐变仅两处合法 | ✅ | 新组件区无渐变滥用；kg/mc 网格点为 `radial-gradient` 网点纹理（DESIGN §二 明文规定），cmp-score/donut 为 conic 数据环（§三 指定同构），均合规 |
| 3 | 状态不只靠颜色 | ✅ | job-steps dot 内含 ✓/序号/▣；cmp-diff 带 ①② 角标；图谱 dim 用透明度；表格状态列全有文字 tag |
| 4 | 阴影克制 | ✅ | 新组件均 `--shadow-sm`，抽屉用 `--shadow`（§三 指定），无 ai-glow 滥用 |
| 5 | 动效从简 | ✅ | 全部 transition .16s–.25s；`prefers-reduced-motion` 全局关闭存在（423 行）；但 closeModal 对该场景无兜底 → 关联 P1-5 |
| 6 | 字号底线 12px | ✅ | 新增组件最小 11px（徽章/辅助，符合令牌字号阶「弱化 11px」）；10px 仅 `.history-step`/`.issue-source`/`.flow-node .n` 三处既有特例，未扩散到新组件 |
| 7 | 新 class 带前缀不污染 | ✅ | nav-/kg-/mc-/cmp-/job-/chain-/lv- 前缀齐；`.drawer` 为 §三 特许；dw-/tb- 两个小工具类未在 §二 名录但语义内聚、无覆盖既有类（可接受） |
| 8 | 演示数据诚实标注 | ⚠️ | 图谱/比对器 EXIF/疑点链已标；**模型画布、比对图占位区未标** → P1-4 |

**门控：8 条红线 6 全过、2 条边缘（均落 P1，无 P0 级违反）。**

---

## 六、返回修订建议（给筑原型）

最小修复集（按优先级）：

1. `switchView` 补 `if (v === "history") renderHistory();`（P1-1，1 行）
2. 委托补 `case "drawerCloseBtn": closeDrawer(); break;`（P1-2，1 行）
3. `closeModal` 加 setTimeout 兜底移除 show（P1-5，2 行）
4. ⬆⬇ 四处替换为内联 SVG（P1-3，~12 行）
5. 模型画布 + 比对图占位区补「演示数据」角标（P1-4，2 行）
6. 模板库 page-sub 补权限说明（P1-6，1 行）

> 审查完成时间：Phase 4 · 临时测试脚本已按要求置于 %TEMP% 并在报告出具后清理。

---

## 七、修订复审记录（第 1 轮）

复审对象：`contract-pro.html`（3059 行，筑原型第 1 轮修订版）。复审方式：静态 grep 核对 + jsdom 复审脚本（17 项断言，16 通过 / 0 JS 错误），覆盖 6 项 P1 关闭验证 + 新建合同向导、预审问题卡、画布追加、图谱高亮 4 项回归。

| P1 | 问题 | 状态 | 复审证据 |
|----|------|:----:|---------|
| P1-1 | 历史台账首进空白 | ✅ 已关闭 | `switchView` 已补 `if (v === "history") renderHistory();`（1713 行）；jsdom 导航直达 history 渲染 8 行 |
| P1-2 | 抽屉底部「关闭」死链 | ✅ 已关闭 | 委托已补 `case "drawerCloseBtn": closeDrawer();`（2471 行）；jsdom 点击后 drawer/mask 均收起 |
| P1-5 | modal 遮罩残留 | ✅ 已关闭 | `closeModal` 已加 260ms setTimeout 兜底（1410 行）；jsdom 审核确认后 mask 无 show 残留，入库流转正常 |
| P1-3 | ⬆⬇ 符号图标 | ✅ 已关闭（残留降为 P2） | 工具箱 3 处 `.up-ic`（1130/1222/1245 行）与项目库导出按钮（1089 行）均已改内联 SVG 上传/下载图标，`.up-ic .ic-svg` 样式补齐（360 行）。**残留**：`tbExport` 按钮 1 处 ⬇（1237 行，构建方已自报）；另既有区（历史/三库/统计/导出弹窗）⬇ 属既有代码豁免范围。残留定为 **P2**，不阻断 |
| P1-4 | 演示数据标注不全 | ✅ 已关闭 | 模型画布参数面板已补「画布连线为静态演示，新增组件不自动布线」（983 行）；比对器图 A/图 B figcaption 均补「演示数据」（1170/1178 行），全比对器共 4 处标注 |
| P1-6 | 权限分级表达偏弱 | ✅ 已关闭 | 模板库页头已加 `tag low`「角色：法务专员」（828 行），page-sub 补「权限：经办人只读 / 法务可编辑（演示）」（829 行） |

**回归抽查**：新建合同向导三步走完（场景卡→要素校验→匹配动画→初稿可编辑）、预审面板 9 张问题卡渲染、模型画布组件追加、图谱点击高亮（3 边 hl）全部通过，无回归。

**复审结论：6/6 P1 关闭，P2 新增 1 项（tbExport ⬇ 残留）。总体结论由「有条件通过」更新为「通过」，可进入 Phase 5-6 导出交付。**
