# 南京江北新区政务AI办公助手产品需求文档（研发交付版）

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 南京江北新区政务AI办公助手 |
| 文档类型 | 产品需求文档（PRD） |
| 适用对象 | 产品、研发、测试、实施、运维、安全及相关业务管理人员 |
| 业务流程依据 | 《南京江北新区政务AI办公助手_全平台业务流程泳道图》 |
| 功能范围依据 | 《南京江北新区政务AI办公助手功能清单》及当前平台原型 |
| 文档状态 | 研发交付稿 |
| 版本 | V1.0 |

## 2. 建设目标与范围

### 2.1 建设目标

建设面向南京江北新区政务办公人员的统一AI办公入口，实现以下能力：

1. 通过统一会话入口接收自然语言、附件、知识文件、智能体及业务连接器等输入。
2. 由主智能体完成意图识别、权限预判和路由，不直接承载具体业务生成。
3. 由智能体中台统一调度12个会话型智能体和5个独立工作台型智能体。
4. 提供普通智能问答、知识问答、智能问数和业务连接器调用能力。
5. 对生成、修改、查询、下载、发送、归档及异常处理全过程留痕。
6. 通过角色权限、部门级数据权限、知识权限和接口权限，防止越权访问和数据泄露。

### 2.2 使用范围

| 用户类别 | 是否允许使用 | 说明 |
|---|---:|---|
| 政务办公使用人员 | 是 | 使用会话、智能体、知识库及已授权业务连接器 |
| 委办局管理员 | 是 | 在使用人员权限基础上，管理本委办局知识和智能体业务配置，查看本委办局统计数据 |
| 系统管理员 | 是 | 管理平台级智能体、连接器、全局规则、模板及平台运行统计 |
| 领导决策人员 | 是 | 作为政务办公使用人员的一类岗位身份，具体功能和数据范围由授权决定，不新增独立管理角色 |
| 企业人员及其他外部人员 | 否 | 身份校验失败时拒绝访问，不展示平台内部资源信息 |

### 2.3 本期功能范围

1. 统一门户、主页会话、新对话及历史对话。
2. 主智能体意图识别和四类智能路由。
3. 12个会话型智能体。
4. 5个独立工作台型智能体。
5. 普通智能问答、知识问答、智能问数。
6. 个人知识库、通用知识库、政策知识库和数据局内部知识库的授权使用。
7. 业务连接器、OA、公文系统和档案系统对接。
8. 任务空间、我的成果、统计报表、个性化和配置管理。
9. 权限控制、内容安全、审计留痕、异常处理。

### 2.4 本期不包含

1. 企业人员注册、登录和使用。
2. 数据查询申请、领导审批或临时授权流程。
3. 任意SQL生成和执行。
4. 未开放数据主题的查询和推断。
5. 合同智能预审；合同智能体本期仅实现合同生成。
6. 对外部系统的自动发送、自动归档；写入类操作必须由用户确认后执行。
7. 未经确认的政策结论、业务数据或内部办理意见自动对外发布。
8. 独立的技能广场和技能后台管理不作为本PRD主流程；如保留“/选择技能”入口，应另行提供技能清单、权限和接口说明。

## 3. 产品原则

1. **统一入口**：用户优先通过自然语言表达需求，不要求先理解系统模块。
2. **明确选择优先**：用户明确选择智能体、知识文件或连接器时，系统优先按选择项处理。
3. **主智能体只路由**：主智能体负责识别、判断和调度；业务处理由智能体中台及对应子智能体完成。
4. **草稿先行**：会话型智能体获得至少一项有效业务信息后即可生成草稿，缺失信息以“XX”占位，支持边问边改。
5. **定稿必校验**：带有必填字段缺失或“XX”占位的文稿不得标记为定稿，不得进入OA发送或档案归档。
6. **复杂任务进工作台**：需要文件解析、多阶段处理或长耗时生成的任务进入独立工作台。
7. **权限取交集**：有效访问范围为用户授权、部门范围、资源授权和数据主题开放范围的交集。
8. **依据可追溯**：知识答案、数据答案和智能体生成结果应保留来源、版本及调用链路。
9. **失败不编造**：知识无可靠依据、数据无返回、外部接口失败时，应如实提示，不得推断或生成虚假结论。

## 4. 系统边界与职责

| 系统/组件 | 核心职责 | 不承担职责 |
|---|---|---|
| 统一门户/对话工作台 | 用户输入、会话展示、附件选择、结果卡片、在线编辑、历史对话 | 不直接调用大模型或业务系统 |
| 身份与权限服务 | 单点登录、用户身份、部门、岗位、角色、菜单和资源权限校验 | 不负责业务内容生成 |
| 主智能体 | 输入理解、意图识别、路由判断、低置信度澄清 | 不直接完成17个业务智能体的生成任务 |
| 智能体中台 | 智能体启停、版本、提示词、模型、规则、工具、知识范围、任务和资源调度 | 不绕过权限直接访问外部数据 |
| 17个业务子智能体 | 按业务场景完成起草、解读、审查、润色、纪要、合同和PPT生成 | 不自行绑定未经中台登记的模型或接口 |
| 知识中台 | 知识空间权限过滤、检索召回、重排、版本及有效性校验 | 用户选择具体文件时不接收“整个知识库”作为生成参数 |
| 文件解析服务 | 获取已授权文件副本，完成DOCX、PDF、XLSX、OCR等解析 | 不扩展用户原有文件权限 |
| 数据中台 | 提供开放主题目录、部门级权限、白名单只读查询、脱敏及结果范围控制 | 不执行任意SQL，不查询未开放主题 |
| 业务连接器 | 连接OA、公文、档案及已登记业务系统，执行授权读写操作 | 未经用户确认不得执行发送、写入或归档 |
| 审计服务 | 记录身份、路由、版本、来源、修改、下载、外部操作和异常 | 不记录无业务必要的明文敏感内容 |

## 5. 角色、菜单和数据权限

### 5.1 角色权限矩阵

| 功能 | 政务办公使用人员 | 委办局管理员 | 系统管理员 |
|---|---:|---:|---:|
| 主页、新对话、历史对话 | 使用本人数据 | 使用本人数据 | 使用本人数据 |
| 智能体广场及已授权智能体 | 使用 | 使用、查看本部门配置 | 全局启停和配置 |
| 业务连接器 | 使用已授权连接器 | 使用已授权连接器 | 全局启停、登记和权限配置 |
| 任务空间 | 查看本人任务 | 查看本人任务；按授权查看本部门统计 | 查看平台运行统计；业务内容仍受数据权限约束 |
| 我的成果 | 管理本人结果 | 管理本人结果 | 管理本人结果 |
| 个人知识库 | 增、删、查、下载、文件夹管理 | 同左 | 同左 |
| 通用知识库 | 查看、使用 | 管理本委办局通用知识库 | 管理全部委办局通用知识库 |
| 政策/内部知识库 | 按授权查看、使用 | 按授权查看、使用或管理 | 按授权管理；不得因系统角色自动获得全部正文权限 |
| 统计报表 | 无或仅本人统计，按实施配置 | 本委办局统计 | 全平台运行统计 |
| 个性化 | 配置允许的个人偏好 | 同左 | 同左 |
| 配置管理 | 不展示管理入口 | 管理本委办局业务配置 | 管理全局配置和各智能体配置 |
| 全局敏感词、行文规范 | 不可管理 | 不可覆盖全局强制规则 | 管理 |
| 长期记忆 | 管理本人长期记忆 | 管理本人长期记忆 | 管理本人长期记忆；不得查看他人记忆正文 |

### 5.2 配置优先级

1. 安全、保密、权限、有效政策、强制行文规范属于强制配置，任何个人偏好不得覆盖。
2. 可叠加规则按“平台全局规则＋委办局补充规则”执行；发生冲突时执行更严格规则并记录命中来源。
3. 个人配置仅作用于允许个性化的展示或生成偏好，如常用讲话人，不改变强制规则。
4. 数据权限不按角色大小简单放大，实际范围为：`用户授权 ∩ 所属部门范围 ∩ 数据主题开放范围 ∩ 字段/明细/导出权限`。
5. 系统管理员拥有平台管理权限，不等同于拥有全部业务数据查看权限。

### 5.3 菜单控制

后端必须返回用户可见菜单和操作权限；前端隐藏仅用于界面展示，不作为权限控制依据。所有接口均须再次校验用户、部门、资源和操作权限。

## 6. 全局业务流程

### 6.1 正向流程

1. 用户通过政务统一身份认证进入平台。
2. 平台获取用户标识、部门标识、岗位、角色及菜单权限。
3. 用户在主页或新对话输入文字，或选择智能体、连接器、具体知识文件及附件。
4. 平台进行文件格式、大小、病毒、敏感内容、密级和上传权限检查。
5. 主智能体结合明确选择、输入语义、附件和历史上下文识别意图。
6. 路由至业务智能体、普通问答、知识问答或智能问数。
7. 对应中台完成权限、启停、版本、并发和资源检查后创建处理任务。
8. 系统生成结果并执行敏感信息、越权数据、引用来源及下载权限检查。
9. 用户查看结果，继续追问、修改、下载、提交OA或归档。
10. 平台记录全过程审计信息。

### 6.2 异常流程

| 异常点 | 处理要求 |
|---|---|
| 身份无效或企业人员访问 | 拒绝访问；不展示平台资源、部门和权限信息；记录失败原因和请求标识 |
| 输入安全校验失败 | 阻断提交；明确提示可处理原因；保留必要审计信息，不进入模型调用 |
| 路由置信度不足 | 使用自然语言一次询问1至2个关键问题；不得直接弹出长表单 |
| 智能体无权限、已停用或繁忙 | 不创建重复任务；提示原因和可执行操作；允许重试或进入对应工作台（如适用） |
| 模型超时或中台异常 | 保留会话、输入和已生成草稿；安全重试使用同一幂等标识；不得重复归档或重复写入 |
| 输出安全校验失败 | 阻断结果交付；返回需人工复核的原因；记录命中规则，不展示受限内容 |
| 用户取消 | 停止后续调用，保留可恢复的草稿或任务状态；记录取消人和时间 |

## 7. 主智能体意图识别与路由

### 7.1 路由类型字典

| 代码 | 名称 | 处理系统 |
|---|---|---|
| AGENT | 业务智能体 | 智能体中台及17个业务子智能体 |
| GENERAL_QA | 普通智能问答 | 受管控的政务本地模型 |
| KNOWLEDGE_QA | 知识问答 | 文件解析服务或知识中台 |
| DATA_QA | 智能问数 | 数据中台 |

### 7.2 路由优先级

1. 用户明确选择具体智能体：路由至该智能体；所选附件或知识文件作为参考材料一并传入。
2. 用户明确选择具体知识文件且未选择业务智能体：路由至知识问答，执行“授权文件读取＋文件解析”。
3. 用户明确选择业务连接器：按连接器授权执行；如同时选择智能体，连接器作为该智能体的受控工具。
4. 未明确选择但问题明显涉及政策、制度或规范：自动路由知识中台检索召回。
5. 输入属于已开放数据主题的指标、趋势、对比、明细或统计查询：路由智能问数。
6. 输入语义明确匹配17个业务智能体：路由对应智能体。
7. 一般解释、办公咨询或非政策制度类问题：路由普通智能问答。
8. 无法可靠判断时：提示用户补充1至2项关键信息，补充后重新识别；不得在低置信度情况下执行外部写入。

### 7.3 复合输入规则

| 场景 | 处理规则 |
|---|---|
| 选择智能体＋附件 | 附件解析结果作为智能体上下文，原文件标识和解析版本进入审计记录 |
| 选择智能体＋知识文件 | 仅读取该具体文件并解析，作为智能体参考材料；不得将整个知识库传给模型 |
| 选择智能体＋连接器 | 智能体为主流程，连接器仅执行用户有权使用的已登记动作 |
| 选择知识文件＋提出政策问题 | 优先依据该文件回答；如需补充检索，须明确提示并再次进行知识空间权限过滤 |
| 同时出现知识和数据意图 | 主智能体先判断用户目标；知识用于解释口径，数据用于返回授权统计；两个来源分别标注 |

## 8. 统一请求字段

| 字段代码 | 字段名称 | 必填 | 类型 | 取值/数据来源 | 校验要求 |
|---|---|---:|---|---|---|
| requestId | 请求标识 | 是 | String | 平台生成，全局唯一 | 用于幂等和链路追踪 |
| traceId | 链路标识 | 是 | String | 审计服务生成 | 贯穿门户、中台、模型和外部系统 |
| conversationId | 对话标识 | 是 | String | 会话服务生成 | 新对话首次提交时生成 |
| userId | 用户标识 | 是 | String | 统一身份认证 | 接口日志按安全要求脱敏 |
| deptId | 部门标识 | 是 | String | 组织机构字典 | 必须为用户当前有效部门 |
| roleCodes | 角色代码 | 是 | Array<String> | 权限服务 | 不接受前端自行传入的角色作为授权依据 |
| inputText | 用户输入 | 条件必填 | String | 用户输入 | 与附件、知识文件至少存在一项 |
| selectedAgentId | 明确选择智能体 | 否 | String | 智能体目录 | 校验启停、版本和使用权限 |
| selectedConnectorId | 明确选择连接器 | 否 | String | 连接器目录 | 校验系统、接口、读写和导出权限 |
| attachmentIds | 会话附件 | 否 | Array<String> | 文件服务 | 单次最多10个；格式、大小待统一文件规范确认 |
| knowledgeFileIds | 知识文件 | 否 | Array<String> | 知识文件目录 | 必须选择具体文件，不允许仅传知识库标识 |
| intentType | 路由类型 | 是 | Enum | AGENT/GENERAL_QA/KNOWLEDGE_QA/DATA_QA | 由主智能体输出，记录置信度 |
| contextVersion | 上下文版本 | 是 | Integer | 会话服务 | 防止并发修改覆盖 |
| clientTime | 客户端提交时间 | 否 | DateTime | 客户端 | 仅作展示，不作为审计权威时间 |

## 9. 会话与交互需求

### 9.1 输入框

1. 首次提示语为“直接描述您的需求，例如：请帮我生成一份发言稿”。
2. 智能体回复后提示语为“继续描述您的需求”。
3. 支持回车发送；换行快捷键由交互规范统一确定并保持全平台一致。
4. 输入“@”展示可用智能体；输入“/”展示已开放快捷能力。
5. 左下角“＋”菜单包含智能体、连接器、知识文件和上传附件。
6. 已选择项在输入框上方以标签展示，可单独移除。
7. 附件单次最多10个；超限时仅保留已确认文件并提示剩余可选数量。

### 9.2 系统回复

1. 所有文字回复和文件结果均显示回复时间，格式为`YYYY-MM-DD HH:mm`。
2. 所有系统回复下方均显示“复制”“追问”文字操作。
3. “复制”复制当前回复正文或文件结果的可复制摘要。
4. “追问”将当前回复作为上下文引用，用户可继续提出修改要求。
5. 生成过程中输入和发送按钮置灰，不允许重复发送；完成或失败后恢复输入。
6. 普通问答回复不展示与业务无关的“本地模型优先”“已留痕可审计”等标语。

### 9.3 生成文件卡片

1. 12个会话型智能体的文稿结果以文件卡片展示，至少包含文件标题和创建时间。
2. 鼠标移入卡片显示下载入口；点击卡片打开右侧在线编辑页。
3. 在线编辑页显示复制、下载、关闭和保存。
4. 保存成功后生成新版本，并同步更新左侧会话中的文件预览内容。
5. 编辑期间保持中间会话输入框可用；用户可展开或收起左侧菜单。
6. 下载、保存、复制和关闭均记录操作日志。

### 9.4 草稿与定稿

| 状态 | 代码 | 进入条件 | 允许操作 |
|---|---|---|---|
| 草稿 | DRAFT | 已生成内容，允许存在“XX”占位 | 查看、编辑、追问、保存、下载草稿 |
| 待确认 | PENDING_CONFIRM | 必填字段已补齐，等待用户确认 | 查看、编辑、确认定稿 |
| 已定稿 | FINAL | 必填字段完整且通过输出校验，用户确认 | 下载、提交OA、归档 |
| 已归档 | ARCHIVED | 档案系统返回成功业务编号 | 查看、按权限下载，不可直接覆盖原版本 |

**统一规则：**至少提供一项有效业务信息即可生成草稿；缺失信息使用“XX”占位。用户要求定稿、提交OA或归档时，系统须集中列出未补齐必填项并阻断操作，不得以默认值替代事实性信息。

## 10. 12个会话型智能体

### 10.1 共性正向流程

1. 用户输入需求或明确选择智能体。
2. 主智能体识别目标智能体并由智能体中台调度。
3. 子智能体从当前输入、历史上下文、附件和具体知识文件提取已知要素。
4. 若无任何有效业务信息，子智能体以自然语言说明可补充的信息，并优先询问一个高价值问题。
5. 若至少存在一项有效业务信息，直接生成可编辑草稿，其他缺失项以“XX”占位。
6. 用户继续提供信息或提出局部修改要求，系统读取当前版本并执行替换、增加、删除或改写。
7. 每次保存生成新版本，保留修改人、修改时间、输入依据和模型/配置版本。
8. 用户确认定稿时执行必填字段、结构、格式、术语、引用和敏感内容校验。

### 10.2 共性异常流程

| 异常 | 处理要求 |
|---|---|
| 未识别到有效信息 | 不生成空泛成文；用自然语言提示并询问一个关键问题 |
| 用户仅回复“好”或“可以” | 如上文已提出模板选择，生成通用空白模板；否则结合上下文确认，不自行推断具体事实 |
| 附件解析失败 | 保留原会话；提示重传、粘贴正文或取消；不得将失败内容传给模型 |
| 用户修改事实字段 | 生成新版本并提示已更新位置；不得覆盖历史版本 |
| 用户要求删除必要内容 | 允许形成草稿，但在定稿校验时提示缺失字段 |
| 模型输出与输入事实冲突 | 标记需人工复核，不得自动定稿 |

### 10.3 智能体字段定义

下表“必填”表示定稿、OA发送和归档前的完整性要求，不作为生成草稿的前置条件。

#### 10.3.1 决定决议智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| subject | 事项名称 | 是 | String | 用户输入 | 去除纯空格 |
| body | 决策主体 | 是 | Enum | 中共南京江北新区工作委员会、南京江北新区管理委员会、管委会主任办公会、部门党组（党委） | 使用智能体业务字典 |
| date | 审议时间 | 是 | Date | 日期控件 | 合法日期 |
| type | 事项类型 | 是 | Enum | 重大事项决策、人事任免、表彰奖惩、重点工作部署、制度文件修订审议 | 使用智能体业务字典 |
| matter | 决定事项 | 是 | Text | 用户输入 | 不得为空或仅为“XX” |

#### 10.3.2 通知通告智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| subject | 通知主题 | 是 | String | 用户输入 | 非空 |
| type | 通知类型 | 是 | Enum | 工作部署、会议活动、事项提醒、公示公告 | 通知类型字典 |
| issuer | 发文单位 | 是 | Enum | 南京江北新区、南京江北新区管理委员会、南京江北新区管委会办公室、南京江北新区数据局、其他部门 | 发文单位字典；选择“其他部门”时补充名称 |
| recipient | 主送单位 | 是 | String | 组织机构字典或用户输入 | 非空 |
| deadline | 完成时限 | 否 | Date | 日期控件 | 合法日期 |
| points | 正文要点 | 是 | Text | 用户输入 | 非空 |

#### 10.3.3 发言稿智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| topic | 讲话主题 | 是 | String | 用户输入 | 非空 |
| speaker | 讲话人及职务 | 否 | String | 讲话人风格库 | 未选择时使用本人常用讲话人；无常用项则为XX |
| occasion | 讲话场合 | 否 | Enum | 自动判断、专题工作汇报、内部会议动员、对外宣传致辞、调研交流座谈、活动开幕致辞、表彰大会讲话 | 讲话场合字典 |
| audience | 听众对象 | 否 | Enum | 自动判断、新区各部门负责人、基层工作人员、企业代表、社会公众、其他 | “企业代表”仅为听众类型，不代表企业人员可登录平台 |
| duration | 预计时长 | 否 | Enum | 自动控制、5分钟、10分钟、15分钟、20分钟 | 时长字典 |
| points | 讲话要点 | 否 | Text | 用户输入 | 事实性内容不得由模型虚构 |

#### 10.3.4 报告请示智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| kind | 文种 | 是 | Enum | 报告、请示 | 文种字典 |
| subject | 主题 | 是 | String | 用户输入 | 非空 |
| issuer | 发文单位 | 是 | Enum | 南京江北新区管委会办公室、南京江北新区经济发展局、南京江北新区数据局、其他部门 | 组织机构字典 |
| recipient | 主送单位 | 是 | String | 组织机构字典或用户输入 | 非空 |
| date | 成文日期 | 是 | Date | 日期控件 | 合法日期 |
| details | 主要内容 | 是 | Text | 用户输入 | 非空 |

#### 10.3.5 工作总结智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| type | 总结类型 | 是 | Enum | 季度工作总结、半年工作总结、年度工作总结、专项工作小结、专题工作汇报、工作复盘报告 | 总结类型字典 |
| period | 总结周期 | 是 | String | 用户输入 | 非空 |
| area | 工作领域 | 是 | Enum | 政务服务、营商环境、数字政务、党建工作、综合类 | 工作领域字典 |
| work | 已完成工作 | 是 | Text | 用户输入或授权材料 | 非空；数据应标明来源 |
| next | 下一步计划 | 否 | Text | 用户输入 | 可为空 |

#### 10.3.6 新闻稿智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| event | 事件/活动名称 | 是 | String | 用户输入 | 非空 |
| date | 发生时间 | 是 | Date | 日期控件 | 合法日期 |
| place | 发生地点 | 是 | String | 用户输入 | 非空 |
| channel | 发布渠道 | 是 | Enum | 政府门户网站、政务公众号、上级媒体报送、内部工作简报 | 发布渠道字典 |
| people | 参与人员 | 否 | String | 用户输入 | 可为空 |
| content | 核心内容 | 是 | Text | 用户输入或授权材料 | 不得编造事实、数据和引语 |

#### 10.3.7 函件智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| kind | 函件类型 | 是 | Enum | 商洽函、询问函、答复函、告知函 | 函件类型字典 |
| sender | 发函单位 | 是 | String | 组织机构字典或用户输入 | 非空 |
| receiver | 收函单位 | 是 | String | 组织机构字典或用户输入 | 非空 |
| subject | 函件主题 | 是 | String | 用户输入 | 非空 |
| deadline | 答复/办理时限 | 否 | Date | 日期控件 | 合法日期 |
| details | 核心事项 | 是 | Text | 用户输入 | 非空 |

#### 10.3.8 工作计划编制

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| type | 计划类型 | 是 | Enum | 年度工作计划、季度工作计划、月度工作计划、专项工作计划 | 计划类型字典 |
| period | 计划周期 | 是 | String | 用户输入 | 非空 |
| issuer | 责任单位 | 是 | Enum | 南京江北新区管理委员会、管委会办公室、经济发展局、数据局、其他部门 | 组织机构字典 |
| goal | 工作目标 | 是 | Text | 用户输入 | 非空 |
| tasks | 重点任务 | 是 | Text | 用户输入 | 非空 |
| measures | 保障措施 | 否 | Text | 用户输入 | 可为空 |

#### 10.3.9 公报公告智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| kind | 类型 | 是 | Enum | 公报、公告 | 公报公告类型字典 |
| publisher | 发布单位 | 是 | String | 组织机构字典或用户输入 | 非空 |
| subject | 主题 | 是 | String | 用户输入 | 非空 |
| date | 发布日期 | 是 | Date | 日期控件 | 合法日期 |
| scope | 发布范围 | 是 | Enum | 面向社会公开、新区内部公开、特定单位和人员 | 发布范围字典；影响输出安全校验 |
| content | 发布内容 | 是 | Text | 用户输入 | 非空 |

#### 10.3.10 批复智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| subject | 批复事项 | 是 | String | 用户输入 | 非空 |
| approver | 批复单位 | 是 | String | 组织机构字典或用户输入 | 非空 |
| requester | 请示单位 | 是 | String | 组织机构字典或用户输入 | 非空 |
| attitude | 批复意见 | 是 | Enum | 同意、原则同意、不同意、请补充材料后再报 | 批复意见字典 |
| basis | 依据 | 否 | String | 用户输入或授权知识来源 | 引用时保留来源 |
| opinion | 具体意见 | 是 | Text | 用户输入 | 非空 |

#### 10.3.11 议案智能生成

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| title | 议案标题 | 是 | String | 用户输入 | 非空 |
| proposer | 提案主体 | 是 | Enum | 南京江北新区管理委员会、南京市人民政府相关部门 | 提案主体字典 |
| recipient | 提请机关 | 是 | Enum | 南京市人民代表大会、南京市人民代表大会常务委员会 | 提请机关字典 |
| meeting | 会议届次 | 是 | String | 用户输入 | 非空，不自动推断届次 |
| date | 提请日期 | 是 | Date | 日期控件 | 合法日期 |
| basis | 提案依据 | 是 | String | 用户输入或授权知识来源 | 非空；保留引用来源 |
| matter | 审议事项 | 是 | Text | 用户输入 | 非空 |

#### 10.3.12 政务文件解读

| 代码 | 字段名称 | 必填 | 类型 | 枚举/数据字典 | 校验 |
|---|---|---:|---|---|---|
| sourceMode | 来源方式 | 是 | Enum | 上传文件、粘贴文字、政策知识库文件 | 选择知识库时必须落实到具体文件 |
| subject | 文件主题 | 是 | String | 用户输入或解析结果 | 非空 |
| audience | 解读对象 | 是 | Enum | 政务工作人员、企业办事人员、社会公众、部门负责人 | 解读对象字典 |
| focus | 解读重点 | 是 | Enum | 核心要点与变化、适用对象与条件、申报材料与办理路径、职责分工与执行要求、风险限制与注意事项 | 解读重点字典 |
| depth | 解读深度 | 是 | Enum | 简明解读、标准解读、详细解读 | 解读深度字典 |
| question | 特别关注问题 | 否 | Text | 用户输入 | 可为空 |

## 11. 发言稿讲话人风格

### 11.1 功能要求

1. 普通使用人员可维护本人有权使用的讲话人风格，并设置一个常用讲话人。
2. 讲话人风格应由讲话人基本信息、授权素材和五个风格维度组成。
3. 五个风格维度为：政治站位与价值表达、结构组织、语言表达、数据与案例使用、情绪与号召方式。
4. 大模型不得在平台内执行模型训练；系统应将已授权素材解析形成的风格摘要及五维参数作为生成上下文或提示词参数。
5. 生成结果文件卡片支持切换其他有权使用的讲话人风格。
6. 切换风格后生成新版本，不覆盖原版本；事实信息、数据和政策引用不得因风格切换而改变。

### 11.2 讲话人风格字段

| 字段代码 | 字段名称 | 必填 | 类型 | 说明 |
|---|---|---:|---|---|
| profileId | 风格档案标识 | 是 | String | 系统生成 |
| speakerName | 讲话人姓名 | 是 | String | 仅在授权范围内展示 |
| speakerTitle | 职务 | 是 | String | 关联组织机构和职务字典 |
| isDefault | 是否常用 | 是 | Boolean | 同一用户仅允许一个常用讲话人 |
| materialFileIds | 参考素材 | 否 | Array<String> | 仅使用已授权材料；支持删除和重新解析 |
| dimensionValues | 五维风格参数 | 是 | Object | 参数取值和展示方式由讲话人风格配置字典确定 |
| styleSummary | 风格摘要 | 是 | Text | 由系统生成后允许用户确认和修改 |
| scopeType | 使用范围 | 是 | Enum | PERSONAL、DEPARTMENT、PLATFORM；权限按角色控制 |
| updatedAt | 更新时间 | 是 | DateTime | 服务端生成 |

### 11.3 异常处理

1. 素材无权限或已失效：停止解析并提示重新选择，不沿用缓存正文。
2. 素材不足以形成稳定风格：保留手工五维配置，提示补充材料，不生成确定性风格结论。
3. 切换讲话人无权限：拒绝切换，不泄露该讲话人素材或风格摘要。
4. 生成后撤销讲话人授权：历史结果按原权限和审计规则保留；后续不得再次调用该风格。

## 12. 5个独立工作台型智能体

### 12.1 通用流程

1. 用户从智能体广场或会话路由进入独立工作台。
2. 智能体中台检查使用权限、启停状态、版本、并发和资源。
3. 用户上传材料、粘贴正文、选择模板或填写必要任务参数。
4. 文件解析成功后创建任务流实例并锁定配置版本。
5. 智能体按“解析—分析—生成/处理—用户确认—输出”执行。
6. 任务空间显示当前节点`当前步骤/总步骤＋节点名称`，如`3/4 合规审查`。
7. 完成后输出文件、报告、问题清单或PPT，并进入我的成果。

### 12.2 通用异常

| 异常 | 处理要求 |
|---|---|
| 文件损坏、加密、格式不支持或OCR质量不足 | 提示重新上传、粘贴正文或取消；不创建重复任务 |
| 智能体繁忙 | 不占用第二个长任务名额；显示排队或稍后重试，具体策略由任务调度配置 |
| 模型或知识服务超时 | 保留任务节点和已生成中间结果；使用同一任务标识安全重试 |
| 用户离开页面 | 后台任务继续执行；任务空间显示状态；完成后产生已读/未读通知 |
| 用户取消 | 停止可停止节点；记录取消状态，不将未完成结果标记为成果 |

### 12.3 辅助文本合规审查

#### 输入字段

| 字段代码 | 字段名称 | 必填 | 类型 | 枚举/来源 | 校验 |
|---|---|---:|---|---|---|
| sourceMode | 输入方式 | 是 | Enum | UPLOAD、PASTE | 二选一 |
| sourceFileId | 待审文件 | 条件必填 | String | DOC、DOCX、PDF；最终范围以文件规范为准 | sourceMode=UPLOAD时必填 |
| sourceText | 待审正文 | 条件必填 | Text | 用户粘贴 | sourceMode=PASTE时必填 |
| detectedDocType | 识别文种 | 是 | String | 文种识别服务 | 低置信度时由用户确认 |
| confirmedDocType | 确认文种 | 是 | String | 文种字典 | 变更后原审查结果失效，须重新审查 |
| reviewScope | 审查范围 | 否 | Array<Enum> | 格式规范、行文规则、政策法规、制度规则、保密规则；默认全选 | 使用全局和部门配置 |

#### 输出

1. 标注问题的正文版本。
2. 问题清单：问题位置、问题类别、风险等级、问题说明、判断依据、整改建议、处理状态。
3. 审查报告：文档信息、审查范围、问题统计、风险结论、生成时间和规则版本。
4. 导出格式和文件名称由管理员配置的审查报告模板确定。

#### 异常

1. 文种变更：现有问题清单和报告标记失效，要求重新审查。
2. 规则依据缺失或冲突：标记“需人工复核”，不得生成确定性合规结论。
3. 高风险命中：是否阻断复核通过由风险分级标准配置，不在前端写死。

### 12.4 文本智能润色

| 字段代码 | 字段名称 | 必填 | 类型 | 枚举/来源 | 校验 |
|---|---|---:|---|---|---|
| sourceMode | 输入方式 | 是 | Enum | UPLOAD、PASTE | 二选一 |
| sourceFileId/sourceText | 待润色文稿 | 是 | String/Text | DOC、DOCX、PDF或粘贴正文 | 至少一个有效输入 |
| docType | 文种 | 否 | Enum | 通用公文、汇报稿、请示报告、会议材料、宣传通稿 | 未选择时自动识别 |
| securityLevel | 密级 | 否 | Enum | NON_SECRET、INTERNAL、SECRET；显示为非涉密、内部、秘密 | 统一密级字典 |
| polishScopes | 润色范围 | 否 | Array<Enum> | 格式与基础规范、政务用语、冗余压缩、风格统一、逻辑结构 | 默认全选 |
| exportFormat | 导出格式 | 是 | Enum | DOCX、PDF、DOCX_PDF_ZIP | 导出时选择 |

输出包括润色稿、修改痕迹和修改说明。金额、日期、机构、责任主体等事实性信息只提供建议，由用户确认后变更。

### 12.5 会议纪要智能生成

| 字段代码 | 字段名称 | 必填 | 类型 | 枚举/来源 | 校验 |
|---|---|---:|---|---|---|
| meetingName | 会议名称 | 是 | String | 用户输入 | 非空 |
| meetingType | 会议类型 | 是 | Enum | 工作例会、专题协调会议、项目推进会议、工作调度会议、部门会商会议 | 会议类型字典 |
| templateId | 纪要模板 | 是 | Enum/ID | 工作例会标准模板、专题协调会议模板、项目推进会议模板、部门会商模板 | 模板须处于有效状态 |
| securityLevel | 密级 | 是 | Enum | NON_SECRET、INTERNAL、SECRET；原型“普通”映射NON_SECRET | 统一密级字典 |
| sourceFileIds/sourceText | 会议材料 | 是 | Array<String>/Text | 音频、会议文件或转写文本 | 至少一种有效来源 |
| meetingTime | 会议时间 | 是 | DateTime | 用户输入或解析 | 合法时间 |
| place | 会议地点 | 否 | String | 用户输入 | 可为空 |
| host | 主持人 | 否 | String | 用户输入 | 可为空 |
| attendees | 参会人员 | 否 | Array<String> | 组织机构/人员字典或用户输入 | 按权限展示 |
| observers | 列席人员 | 否 | Array<String> | 用户输入 | 可为空 |
| recorder | 记录人 | 否 | String | 用户输入 | 可为空 |
| remark | 备注 | 否 | Text | 用户输入 | 可为空 |

输出包括会议概况、主要议题、议定事项、任务清单和按授权保留的转写依据。任务提取规则由智能体内置处理，不作为普通用户配置项。

### 12.6 合同智能生成

本期只实现合同生成，不实现合同预审、合规评分或自动法务结论。

| 字段代码 | 字段名称 | 必填 | 类型 | 枚举/来源 | 校验 |
|---|---|---:|---|---|---|
| contractName | 合同名称 | 是 | String | 用户输入 | 非空 |
| cooperationType | 合作类型 | 是 | Enum | 政企合作、战略合作、项目共建、服务委托、联合运营 | 合作类型字典 |
| securityLevel | 密级 | 否 | Enum | NON_SECRET、INTERNAL、SECRET | 统一密级字典 |
| partyA | 甲方 | 是 | String | 当前部门或授权主体 | 默认系统带出，用户按权限修改 |
| partyB | 乙方 | 是 | String | 用户输入 | 非空 |
| partyBPhone | 乙方联系电话 | 是 | String | 用户输入 | 格式校验，不校验真实性 |
| totalAmount | 合同金额（万元） | 是 | Decimal | 用户输入 | 大于等于0 |
| startDate | 开始日期 | 是 | Date | 日期控件 | 合法日期 |
| endDate | 结束日期 | 是 | Date | 日期控件 | 晚于开始日期 |
| background | 合作背景与目标 | 否 | Text | 用户输入 | 可为空 |
| responsibilities | 权责分工 | 否 | Text | 用户输入 | 可为空 |
| riskRequirements | 合规与风险要求 | 是 | Array<String> | 用户选择或输入 | 至少一项 |
| templateId | 合同模板 | 是 | String | 有效合同模板库 | 仅可使用有权查看的生效模板 |

输出为可编辑合同草稿和使用模板信息。任何“法律合规”“风险通过”等结论均不属于本期输出。

### 12.7 PPT智能生成

| 字段代码 | 字段名称 | 必填 | 类型 | 枚举/来源 | 校验 |
|---|---|---:|---|---|---|
| themeInput | 主题或素材说明 | 是 | Text | 用户输入 | 非空 |
| title | 主标题 | 是 | String | 用户输入 | 非空 |
| subtitle | 副标题 | 否 | String | 用户输入 | 可为空 |
| pptType | PPT类型 | 是 | Enum | 工作汇报、政策宣讲、年终总结、自定义 | PPT类型字典 |
| audience | 受众 | 否 | String | 用户输入 | 可为空 |
| scene | 使用场景 | 否 | String | 用户输入 | 可为空 |
| sourceFileIds | 参考材料 | 否 | Array<String> | 授权附件或知识文件 | 单次最多10个 |
| templateId | PPT模板 | 是 | String | 用户选择或系统默认有效模板 | 锁定模板版本 |

PPT属于长耗时任务。同一用户同一时间最多存在一个运行中的PPT生成任务；已存在运行任务时禁止创建第二个，并提供进入现有任务入口。

## 13. 普通智能问答

### 13.1 正向流程

1. 主智能体确认问题为一般解释或办公咨询，且不涉及政策制度检索、业务数据查询和业务智能体任务。
2. 调用受管控的政务本地模型生成答复。
3. 输出前执行敏感内容和越权信息检查。
4. 页面显示“内容由AI生成，请核实重要信息”。

### 13.2 异常流程

1. 问题实际涉及政策制度：转知识问答，不以普通模型直接下结论。
2. 问题涉及未授权内部信息：拒绝回答，不确认受限信息是否存在。
3. 模型无法可靠回答：明确说明无法确认，建议补充材料或转对应智能体。

## 14. 知识问答

### 14.1 具体知识文件模式

#### 正向流程

1. 用户在对话框逐级进入知识目录并选择具体文件。
2. 系统校验用户、部门、知识空间、目录、文件状态和下载/读取权限。
3. 文件服务读取授权文件副本并调用解析服务。
4. 解析结果分段后加入当前会话上下文。
5. 模型仅基于授权文件内容和当前问题生成回答，并标注文件名称、版本和位置。

#### 异常流程

| 异常 | 处理 |
|---|---|
| 文件无权限或已删除 | 拒绝读取，不展示文件正文和历史缓存 |
| 文件加密、损坏或格式不支持 | 提示重新上传、粘贴正文或取消 |
| 扫描件质量不足 | 提示OCR识别质量不足，要求更换文件或人工确认 |
| 文件内容无答案 | 如实说明未在所选文件中找到依据，不自动扩展至全知识库 |

### 14.2 知识中台检索模式

#### 触发条件

1. 用户未选择具体文件，但明确提出政策、制度或规范问题。
2. 主智能体根据语义识别为政策制度类问题。

#### 正向流程

1. 按个人、通用、政策和部门内部知识空间执行权限过滤。
2. 完成查询改写、关键词/向量召回、重排。
3. 校验文档版本、有效状态、适用范围和替代关系。
4. 仅使用用户有权访问的片段生成答复。
5. 标注文件名称、文号（如知识元数据存在）、版本、位置及更新时间。

#### 异常流程

1. 无可靠召回结果：不得给出确定结论，提示缺少依据。
2. 多个来源冲突：展示冲突来源和差异，提示人工核验。
3. 文档失效或存在替代关系：不得继续以失效文档作为现行依据。
4. 知识服务不可用：保留问题并允许重试，不转普通模型编造政策答案。

### 14.3 知识问答字段

| 字段代码 | 字段名称 | 必填 | 类型 | 说明 |
|---|---|---:|---|---|
| mode | 知识模式 | 是 | Enum | FILE、RETRIEVAL |
| fileIds | 具体知识文件 | 条件必填 | Array<String> | mode=FILE时必填 |
| query | 用户问题 | 是 | Text | 非空 |
| allowedSpaceIds | 授权知识空间 | 是 | Array<String> | 后端根据权限生成 |
| sourceFragments | 召回片段 | 是 | Array<Object> | 包含文件、版本、位置、分值和有效状态 |
| answer | 生成答案 | 是 | Text | 仅可使用授权来源 |
| citationStatus | 引用状态 | 是 | Enum | COMPLETE、PARTIAL、NONE、CONFLICT |

## 15. 智能问数

### 15.1 开放主题

一期仅开放以下数据主题：经济运行、项目建设、企业服务、政务服务、部门内部台账。

### 15.2 权限规则

1. 数据权限控制到部门级。
2. 不存在申请授权或领导审批流程。
3. 用户不具备权限时直接拒绝，不展示目标数据是否存在。
4. 部分人员可查看和导出明细，具体以字段级、明细级和导出权限为准。
5. 无明细权限时仅返回授权聚合结果，并执行脱敏和小样本保护。

### 15.3 正向流程

1. 主智能体提取数据主题、指标、维度、时间范围和部门范围。
2. 数据中台校验主题开放状态、用户部门范围、指标权限、明细权限和导出权限。
3. 自然语言映射至白名单只读接口或查询模板，不生成和执行任意SQL。
4. 返回汇总、图表或授权明细。
5. 结果标明指标口径、数据来源、更新时间和权限范围。

### 15.4 异常流程

| 异常 | 处理要求 |
|---|---|
| 主题未开放或未接入 | 明确说明当前不可查询；不得推断或生成模拟数据 |
| 指标、时间或维度不明确 | 询问一个关键澄清问题后再查询 |
| 查询范围过大 | 提示缩小时间、部门或指标范围 |
| 无数据返回 | 如实显示无数据，保留查询条件 |
| 接口超时 | 允许安全重试；不得重复导出或生成不同口径结果 |
| 无明细/导出权限 | 返回授权汇总；隐藏明细和导出入口 |

### 15.5 智能问数字段

| 字段代码 | 字段名称 | 必填 | 类型 | 说明 |
|---|---|---:|---|---|
| topicCode | 数据主题 | 是 | Enum | ECONOMIC_OPERATION、PROJECT_CONSTRUCTION、ENTERPRISE_SERVICE、GOV_SERVICE、DEPT_LEDGER |
| metricCodes | 指标 | 是 | Array<String> | 关联数据中台指标目录 |
| dimensions | 维度 | 否 | Array<String> | 仅允许主题已开放维度 |
| timeRange | 时间范围 | 是 | Object | 起止时间或统计期 |
| deptScope | 部门范围 | 是 | Array<String> | 后端根据用户权限生成 |
| detailRequired | 是否请求明细 | 是 | Boolean | 用户意图 |
| detailAllowed | 是否允许明细 | 是 | Boolean | 权限服务返回 |
| exportAllowed | 是否允许导出 | 是 | Boolean | 权限服务返回 |
| queryTemplateId | 查询模板 | 是 | String | 数据中台白名单模板 |
| dataUpdatedAt | 数据更新时间 | 是 | DateTime | 数据中台返回 |

## 16. 业务连接器、OA、公文和档案系统

### 16.1 权限维度

连接器权限至少包含：连接器使用权限、接口权限、读取权限、写入权限和导出权限。写入权限不得由读取权限推导。

### 16.2 正向流程

1. 用户明确选择连接器或在智能体流程中触发连接器工具。
2. 后端校验用户、部门、连接器启停、具体接口和操作权限。
3. 读取类操作执行后，将授权结果返回当前会话或任务。
4. OA发送、公文提交和档案归档前展示目标系统、目标对象、文件版本和操作摘要。
5. 用户确认后执行写入。
6. 外部系统返回业务编号、处理状态和时间；平台保存对应关系。

### 16.3 异常流程

1. 无权限：拒绝操作，不泄露目标系统内部数据。
2. 连接器停用：提示当前不可用，保留本地结果供下载。
3. 外部系统超时：任务保持“待确认/执行失败”，允许使用同一幂等标识重试。
4. 外部系统返回失败：保存错误代码和可展示原因，不将本地结果标记为已发送或已归档。
5. 重复提交：通过`requestId＋业务动作＋结果版本`进行幂等控制。

## 17. 知识库管理

### 17.1 个人知识库

1. 用户可在左侧目录树创建、重命名和删除本人文件夹。
2. 选中文件夹后可上传多个文件，单次最多10个。
3. 用户可查看、下载和删除本人文件。
4. 删除非空文件夹前须二次确认；后端应明确采用禁止删除或级联删除策略，最终以知识中台接口能力为准。
5. 知识目录和文件操作均记录审计日志。

### 17.2 其他知识库

1. 普通使用人员仅查看和使用授权内容。
2. 委办局管理员管理本委办局通用知识库。
3. 系统管理员管理全部委办局通用知识库，但政策和内部知识正文仍按资源授权控制。
4. 列表统一展示文件名称、更新时间和操作；“查看”进入在线预览。

## 18. 任务空间与通知

### 18.1 任务流实例字段

| 字段代码 | 字段名称 | 必填 | 类型 | 说明 |
|---|---|---:|---|---|
| taskId | 任务标识 | 是 | String | 系统生成 |
| taskName | 任务名称 | 是 | String | 由智能体和输入主题生成 |
| agentId | 智能体标识 | 是 | String | 仅展示智能体任务，不展示连接器独立任务 |
| status | 状态 | 是 | Enum | PENDING、RUNNING、COMPLETED、FAILED、CANCELLED |
| currentStep | 当前步骤 | 是 | Integer | 从1开始 |
| totalSteps | 总步骤 | 是 | Integer | 大于等于currentStep |
| currentNodeName | 当前节点 | 是 | String | 页面展示`currentStep/totalSteps 节点名称` |
| createdAt | 创建时间 | 是 | DateTime | 服务端时间 |
| completedAt | 完成时间 | 否 | DateTime | 完成、失败或取消时写入 |
| resultId | 结果标识 | 否 | String | 完成后关联我的成果 |
| unread | 完成未读 | 是 | Boolean | 打开任务流列表并展示完成项后置为false |

### 18.2 页面规则

1. 任务空间只保留任务流实例，隐藏定时任务和运行记录。
2. 列表仅展示智能体任务，操作仅保留“进入智能体”。
3. 主页任务流侧栏最多展示3个运行中任务；不展示不确定的剩余时间和无关副标题。
4. 已完成未读数量显示在任务流入口；用户打开并看到完成任务后清零相应未读状态。
5. 完成任务置于侧栏最上方，随后按完成时间倒序。

## 19. 我的成果与历史对话

### 19.1 我的成果

1. 仅展示智能体或连接器生成的成果。
2. 字段包括成果名称、生成来源、更新时间、关联对话和操作；不展示审批状态和成果编号。
3. 查看弹窗不展示审批状态和成果编号。
4. “上传到个人知识库”将当前成果作为新文件上传，需校验知识空间权限。
5. 生成结果自动归档到我的成果；“我的成果”文字可点击跳转。

### 19.2 历史对话

1. 首页最近对话最多显示20条，保留“查看历史对话”。
2. 管理历史对话页面每条仅显示主标题和时间。
3. 点击条目打开原对话；删除操作须二次确认。
4. 删除后会话不可在用户界面恢复；审计记录按项目审计制度保留，保留期限待安全管理要求确认。

## 20. 个性化与配置管理

### 20.1 普通使用人员个性化

1. 页面采用“左侧选择智能体、右侧显示该智能体配置”的布局。
2. 不需要个人配置的智能体不展示。
3. 每个智能体最多保留一个动态个人配置项。
4. 当前个人配置范围：发言稿智能生成配置“常用讲话人”；其他智能体如无已确认个人偏好，不设置配置项。
5. 删除“政务AI助手人设提示词”。

### 20.2 管理员配置

#### 全局配置

1. 敏感词和保密规则。
2. 通用行文规范、版式规范、文种及文种别名。
3. 组织机构、政务术语、统一口径和数据字典。
4. 风险分级、输出模板和全局文件规范。

#### 智能体配置

| 智能体 | 管理配置 |
|---|---|
| 11个起草类智能体 | 公文格式模板查看；除发言稿外不增加普通用户配置 |
| 发言稿智能生成 | 公文格式模板查看、讲话人风格管理 |
| 辅助文本合规审查 | 支持文种及格式模板、行文规则库、政策法规库、制度规则库、保密规则库、版式要求、文种识别匹配、风险分级、审查报告模板 |
| 文本智能润色 | 文种版式要素、格式规则、标准术语、替换词、表达规则、机构统一口径、冗余规则、逻辑结构规则、输出模板 |
| 通知通告智能生成 | 通知类型、发文单位、通告版式模板 |

配置项不向用户展示内部版本号；后端仍须保存配置版本，用于任务锁定和审计追溯。

## 21. 逻辑接口清单

本节定义对接职责和字段，不定义实际URL、协议地址或厂商实现。实际接口文档由对应系统提供后补充。

| 接口编号 | 接口名称 | 调用方→提供方 | 触发时机 | 主要请求字段 | 主要返回字段 | 权限/异常要求 |
|---|---|---|---|---|---|---|
| IAM-01 | 获取当前用户上下文 | 门户→统一身份/权限 | 登录成功 | loginToken、requestId | userId、deptId、岗位、角色、菜单、资源权限 | 企业人员或失效账号拒绝 |
| AGT-01 | 主智能体路由 | 门户→主智能体 | 用户提交 | 统一请求字段、上下文摘要 | intentType、targetAgentId、confidence、clarifyQuestion | 低置信度不得执行外部写入 |
| AGT-02 | 创建智能体任务 | 主智能体→智能体中台 | 路由为AGENT | requestId、agentId、userContext、input、references | taskId、configVersion、status | 校验权限、启停、并发和资源 |
| AGT-03 | 查询任务状态 | 门户→智能体中台 | 任务运行中 | taskId、userId | status、currentStep、totalSteps、node、resultId | 仅任务本人或授权管理范围可查 |
| FILE-01 | 上传文件 | 门户→文件服务 | 用户选文件 | file、userId、deptId、securityLevel | fileId、name、size、format、scanStatus | 单次最多10个；安全检查失败阻断 |
| FILE-02 | 解析文件 | 智能体/知识流程→解析服务 | 文件通过校验 | fileId、parseMode、requestId | text、segments、metadata、quality | 损坏/加密/低质量返回明确状态 |
| KMS-01 | 获取知识目录和文件 | 门户→知识中台 | 用户选择知识文件 | userId、deptId、parentId | folders、files、permission | 不返回无权目录和文件 |
| KMS-02 | 读取具体知识文件 | 知识流程→知识中台/文件服务 | 已选择文件 | fileId、userContext | authorizedCopyId、metadata | 重新校验实时权限 |
| KMS-03 | 知识检索召回 | 主智能体→知识中台 | 政策制度问题 | query、allowedSpaceIds、userContext | fragments、sources、validity、conflicts | 先权限过滤后召回 |
| DATA-01 | 获取开放数据目录 | 主智能体→数据中台 | 智能问数识别 | userId、deptId | topics、metrics、dimensions、permission | 仅返回授权目录 |
| DATA-02 | 执行受控查询 | 主智能体→数据中台 | 查询条件明确 | topic、metrics、dimensions、timeRange、deptScope、templateId | summary、chartData、details、source、updatedAt | 白名单只读；脱敏；字段和导出控制 |
| CONN-01 | 获取可用连接器 | 门户→连接器平台 | 展开连接器选择 | userId、deptId | connectorId、name、allowedActions | 隐藏未授权连接器 |
| CONN-02 | 执行连接器动作 | 智能体/门户→连接器平台 | 用户确认动作 | requestId、connectorId、action、payload、resultVersion | businessNo、status、message | 写入类二次确认、幂等 |
| OA-01 | 提交OA/公文系统 | 连接器平台→OA/公文系统 | 用户确认发送 | fileId、resultVersion、target、metadata | businessNo、status、submittedAt | 失败不标记已发送 |
| ARCH-01 | 档案归档 | 连接器平台→档案系统 | 用户确认归档 | fileId、resultVersion、archiveMetadata | archiveNo、status、archivedAt | 失败保留本地成果 |
| RESULT-01 | 保存成果版本 | 门户/智能体→成果服务 | 生成或编辑保存 | resultId、conversationId、taskId、content、versionBase | resultId、version、savedAt | 乐观锁防覆盖 |
| AUDIT-01 | 写入审计事件 | 各组件→审计服务 | 关键操作发生 | traceId、actor、resource、action、result、sourceVersion | auditId、storedAt | 失败应告警；关键写入是否阻断待安全方案确认 |

## 22. 状态与幂等

### 22.1 任务状态

| 状态 | 说明 | 可迁移状态 |
|---|---|---|
| PENDING | 已创建待执行 | RUNNING、CANCELLED、FAILED |
| RUNNING | 正在执行 | COMPLETED、FAILED、CANCELLED |
| COMPLETED | 执行完成 | 终态 |
| FAILED | 执行失败 | PENDING（重试时创建重试次数，不重复创建业务结果） |
| CANCELLED | 用户或系统取消 | 终态；如需重做创建新任务 |

### 22.2 幂等规则

1. 用户发送：`requestId`唯一；重复请求返回原任务或原响应。
2. 保存版本：使用`resultId＋baseVersion`乐观锁；版本冲突提示刷新后重试。
3. OA发送/归档：使用`requestId＋action＋resultVersion`；同一结果版本不得重复写入。
4. 长任务重试：沿用原`taskId`记录重试次数，或由中台返回明确的父子任务关系。

## 23. 内容安全、数据合规与审计

### 23.1 输入安全

1. 文件病毒、格式、大小和数量校验。
2. 用户、部门、密级和上传权限校验。
3. 敏感内容和涉密风险检查。
4. 检查失败时不得进入模型和外部系统。

### 23.2 输出安全

1. 敏感信息、涉密内容和公开范围检查。
2. 越权数据和无权限知识来源检查。
3. 政策引用来源、版本、有效状态和冲突检查。
4. 下载、明细、OA发送和归档权限检查。
5. AI生成内容统一提示“内容由AI生成，请核实重要信息”。

### 23.3 审计字段

| 字段 | 必填 | 说明 |
|---|---:|---|
| traceId、requestId | 是 | 全链路和单次请求标识 |
| conversationId、taskId、resultId | 条件必填 | 对话、任务和成果关联 |
| userId、deptId、roleCodes | 是 | 操作主体及组织范围 |
| intentType、targetAgentId | 是 | 路由结果 |
| agentVersion、modelVersion、configVersion | 条件必填 | 智能体任务版本锁定 |
| knowledgeSources、dataSources | 条件必填 | 知识和数据来源及版本 |
| operation | 是 | 创建、修改、复制、下载、发送、归档、删除、重试等 |
| operationResult | 是 | 成功、失败、拒绝、取消 |
| errorCode | 否 | 失败或拒绝时记录 |
| serverTime | 是 | 权威操作时间 |

审计保留期限、脱敏范围和日志查阅权限按项目安全与档案管理要求配置，本PRD不擅自设定期限。

## 24. 异常码及页面反馈

| 异常码 | 场景 | 页面提示原则 | 系统处理 |
|---|---|---|---|
| AUTH-001 | 身份无效/外部人员 | 无权访问本平台 | 拒绝并记录，不泄露资源 |
| AUTH-002 | 功能或资源无权限 | 当前账号无此操作权限 | 拒绝接口调用 |
| INPUT-001 | 输入为空 | 请输入需求或添加附件/知识文件 | 不创建请求 |
| INPUT-002 | 文件安全校验失败 | 展示可处理原因 | 删除临时上传或隔离 |
| ROUTE-001 | 路由置信度不足 | 询问1至2个关键问题 | 重新识别 |
| AGENT-001 | 智能体停用 | 当前智能体暂不可用 | 不创建任务 |
| AGENT-002 | 智能体繁忙 | 当前任务较多，请稍后重试或进入现有任务 | 执行并发策略 |
| AGENT-003 | 模型/中台超时 | 本次处理未完成，可继续重试 | 保留草稿和幂等标识 |
| FILE-001 | 文件损坏/加密/不支持 | 请重新上传或粘贴正文 | 不传模型 |
| FILE-002 | OCR质量不足 | 文件识别质量不足，请更换文件 | 保留原文件标识和失败状态 |
| KB-001 | 知识无可靠结果 | 未找到可作为依据的内容 | 不生成确定性结论 |
| KB-002 | 知识来源冲突 | 检索到不同版本或口径，请人工核验 | 展示有权查看的冲突来源 |
| DATA-001 | 数据主题未开放 | 当前数据主题暂不支持查询 | 不生成模拟数据 |
| DATA-002 | 数据无权限 | 当前账号无此数据访问权限 | 不确认数据是否存在 |
| DATA-003 | 查询无数据 | 当前条件下未查询到数据 | 保留条件供修改 |
| CONN-001 | 连接器不可用 | 外部系统暂不可用，结果已保留 | 允许重试或本地下载 |
| CONN-002 | 外部写入失败 | 提交未成功，请重试或人工处理 | 不标记已发送/归档 |
| OUTPUT-001 | 输出安全校验失败 | 结果需人工复核，暂不可交付 | 阻断下载、发送或归档 |
| VERSION-001 | 编辑版本冲突 | 内容已更新，请刷新后重试 | 不覆盖较新版本 |

## 25. 非功能要求

### 25.1 安全

1. 所有权限在服务端校验。
2. 传输、存储、日志和缓存的安全方式按项目安全方案执行。
3. 模型、中台和连接器调用不得扩大原始用户权限。
4. 敏感信息不得以错误栈、调试信息或前端配置形式暴露。

### 25.2 可用性

1. 长任务须显示当前节点和状态，不展示无法保证的剩余时间。
2. 页面刷新或重新登录后，可恢复本人未完成任务和已保存草稿。
3. 外部服务异常不应导致本地成果丢失。
4. 发送完成后输入框恢复可用，已选智能体、连接器、知识文件和附件恢复初始状态；当前对话上下文继续保留。

### 25.3 性能和容量

1. 普通问答、路由识别、知识检索、数据查询和长任务分别配置超时阈值。
2. 具体响应时间、并发量、文件大小、OCR页数和日志容量指标须由项目性能方案和部署资源确认后补入，不在PRD中虚构数值。
3. PPT等长任务执行单用户并发控制；其他智能体并发策略由智能体中台配置。

### 25.4 可维护性

1. 智能体、模型、提示词、规则、模板、知识范围和连接器均保存独立版本。
2. 任务创建时锁定版本，运行期间不得因管理员修改配置而改变处理结果。
3. 配置发布、回滚和停用须记录操作人、时间、影响范围和版本。

## 26. 验收标准

### 26.1 权限验收

1. 企业人员和无效账号无法进入平台。
2. 三类角色菜单、操作和配置范围与权限矩阵一致。
3. 前端隐藏后直接调用接口仍被服务端拒绝。
4. 系统管理员不因平台角色自动获得未授权业务明细。
5. 无权知识、数据、讲话人风格和连接器不出现在选择列表，直接传标识同样被拒绝。

### 26.2 路由验收

1. 明确选择知识文件必走文件读取和解析。
2. 未选文件的政策制度问题走知识中台。
3. 一般解释和办公咨询走普通问答。
4. 五类开放数据主题走智能问数；未开放主题不返回模拟数据。
5. 17个业务意图均通过智能体中台调度，不由主智能体直接生成。
6. 低置信度场景只询问1至2项关键信息，不弹出长表单。

### 26.3 会话型智能体验收

1. 用户仅提供一项有效信息可生成带“XX”占位的草稿。
2. 用户回复新的业务信息后，系统更新当前文稿并形成新版本。
3. 用户提出局部修改时，仅修改目标段落或字段，未要求修改的事实保持不变。
4. 存在必填缺失或“XX”时，定稿、OA发送和归档被阻断。
5. 文件卡片展示标题和创建时间，支持下载、打开编辑、复制和追问。
6. 在线编辑保存后，左侧会话文件内容同步更新。
7. 发言稿切换讲话人风格后生成新版本，事实和引用不被改变。

### 26.4 独立工作台验收

1. 5个工作台均执行权限、文件、版本和任务状态校验。
2. 文件解析失败时支持重传、粘贴或取消，不生成重复任务。
3. 任务空间显示`步骤/总步骤＋节点名称`。
4. PPT同一用户最多一个运行中生成任务。
5. 合同智能体只输出合同草稿，不输出合同预审或合规通过结论。

### 26.5 知识、数据和连接器验收

1. 知识回答标明来源；无可靠来源或来源冲突时不下确定结论。
2. 数据回答标明指标口径、来源、更新时间和权限范围。
3. 无明细权限时不返回明细和导出入口。
4. OA发送和归档必须二次确认；外部失败时本地结果保留且状态不误报。
5. 重复提交不会产生重复OA业务或重复档案记录。

### 26.6 审计验收

1. 可按`traceId`还原一次请求从入口、路由、中台、模型、知识/数据来源到交付的完整链路。
2. 生成、修改、下载、发送、归档、删除、拒绝、重试和异常均有记录。
3. 审计记录包含用户、部门、智能体/模型/配置版本和服务端时间。

## 27. 测试场景清单

| 编号 | 场景 | 预期结果 |
|---|---|---|
| T-01 | 企业人员尝试登录 | 拒绝访问，不展示内部菜单和资源 |
| T-02 | 用户输入“帮我生成一份函件” | 识别函件智能体，说明可补充信息并询问一个关键问题 |
| T-03 | 用户随后回复“商洽函” | 生成商洽函草稿，其他未知字段以XX占位 |
| T-04 | 用户回复“好”接受通用模板 | 生成通用空白函件模板 |
| T-05 | 用户补充收发函单位 | 更新原草稿并生成新版本 |
| T-06 | 草稿含XX时点击OA发送 | 阻断并集中提示未补齐必填项 |
| T-07 | 选择政策知识文件提问 | 读取该文件并解析，不调用整个知识库检索 |
| T-08 | 未选文件询问政策制度 | 进入知识中台检索，标注来源 |
| T-09 | 知识来源冲突 | 展示冲突说明，提示人工核验，不下确定结论 |
| T-10 | 查询经济运行授权汇总 | 返回汇总、口径、来源和更新时间 |
| T-11 | 无明细权限请求企业服务明细 | 仅返回授权汇总，不显示明细和导出 |
| T-12 | 查询未开放业务数据 | 明确不支持，不生成模拟数据 |
| T-13 | PPT已有运行任务时再次创建 | 阻止创建第二个任务并提供进入现有任务入口 |
| T-14 | 上传第11个附件 | 阻止超限文件，保留前10个并提示上限 |
| T-15 | 文件损坏或加密 | 不进入模型，提示重传或粘贴 |
| T-16 | 编辑结果时发生版本冲突 | 不覆盖新版本，提示刷新重试 |
| T-17 | OA提交超时后重复点击 | 幂等返回原状态或安全重试，不产生重复业务 |
| T-18 | 合同工作台完成生成 | 仅输出合同草稿，不出现预审评分或通过结论 |
| T-19 | 发言稿切换讲话人风格 | 新建结果版本，事实字段保持一致 |
| T-20 | 打开任务流侧栏查看完成任务 | 完成任务置顶，对应未读数量清零 |

## 28. 研发交付前置资料

以下资料缺失时，相应接口和验收数据不得以模拟结果替代正式对接：

1. 统一身份认证、组织机构、岗位和角色接口文档。
2. 菜单、操作、资源及部门级数据权限模型。
3. 17个智能体正式标识、启停规则、模型清单、提示词/规则版本规范。
4. 12个会话型智能体字段字典和有效公文模板文件。
5. 5个独立工作台支持的文件格式、单文件大小、页数和音视频时长限制。
6. 密级、公开范围、文种、组织机构、通知类型、风险等级等统一数据字典。
7. 辅助文本合规审查规则库、风险分级及输出模板。
8. 文本润色术语、替换词、表达规则、机构名称和输出模板。
9. 发言稿讲话人授权范围、素材文件、五维风格取值及使用期限。
10. 知识中台目录、文件、上传、删除、下载、权限、检索、版本和有效性接口文档。
11. 文件解析服务支持格式、OCR能力、错误码和结果结构。
12. 数据中台一期五类主题的指标目录、口径、维度、白名单查询模板、更新时间、明细及导出权限。
13. 业务连接器清单、每个连接器动作、读写权限、错误码和幂等规则。
14. OA、公文系统和档案系统接口文档、必填元数据和业务状态字典。
15. 审计日志字段、脱敏、查阅权限和保留期限要求。
16. 性能、容量、可用性、备份恢复和部署资源指标。

## 29. 需求追踪矩阵

| 需求模块 | 泳道图对应页 | 本PRD章节 |
|---|---|---|
| 统一入口与身份权限 | 01 | 5、6、8 |
| 主智能体四类路由 | 01 | 7 |
| 12个会话型智能体 | 02 | 9、10、11 |
| 5个独立工作台 | 02 | 12 |
| 普通智能问答 | 01 | 13 |
| 知识文件与知识中台 | 01、03 | 14、17 |
| 智能问数与部门级权限 | 01、03 | 15 |
| 业务连接器、OA、公文、档案 | 01、03 | 16 |
| 任务、成果和历史对话 | 01、02 | 18、19 |
| 配置管理与个性化 | 原型及功能清单 | 20 |
| 接口、异常、审计和验收 | 01、02、03 | 21—28 |

## 30. 研发需求拆分与依赖顺序

| 工作包 | 建设内容 | 前置依赖 | 可交付结果 |
|---|---|---|---|
| WP-01 身份权限 | 单点登录、组织部门、三类角色、菜单、操作、资源和部门级数据权限 | 统一身份及权限接口文档 | 用户上下文服务、权限拦截、角色菜单 |
| WP-02 会话基础 | 新对话、历史对话、上下文、输入框、附件、知识文件选择、文件卡片和右侧编辑 | WP-01、文件规范 | 可独立运行的统一会话工作台 |
| WP-03 主智能体路由 | 四类意图、优先级、置信度、澄清及复合输入处理 | WP-01、智能体目录、知识和数据目录 | 可追踪的路由结果和澄清流程 |
| WP-04 智能体中台接入 | 权限、启停、版本锁定、模型/提示词/规则/工具配置、任务调度 | WP-03、中台接口 | 17个子智能体统一调度链路 |
| WP-05 12个会话型智能体 | 字段提取、XX占位、边问边改、定稿校验、文件卡片、成果版本 | WP-02、WP-04、公文模板 | 12个会话型智能体闭环 |
| WP-06 5个独立工作台 | 文件解析、多阶段任务、状态节点、结果输出、PPT单任务并发 | WP-04、文件解析、任务服务 | 5个工作台闭环 |
| WP-07 知识问答 | 具体文件读取解析、知识权限、检索召回、重排、有效性和引用 | WP-01、知识中台、文件解析 | 两种知识问答模式 |
| WP-08 智能问数 | 五类主题、指标目录、白名单查询、部门权限、脱敏、明细和导出控制 | WP-01、数据中台指标及权限目录 | 授权数据问答结果 |
| WP-09 连接器协同 | 连接器目录、读写授权、OA、公文、档案确认、幂等及状态回传 | WP-01、外部系统接口 | 受控读取、发送和归档 |
| WP-10 任务与成果 | 任务流实例、完成未读、我的成果、下载、个人知识库上传 | WP-04、成果服务 | 任务与结果管理闭环 |
| WP-11 配置与个性化 | 全局规则、部门配置、模板、讲话人风格、配置版本和发布 | WP-01、WP-04、正式字典/模板 | 分角色配置页面和有效配置服务 |
| WP-12 安全审计与验收 | 输入/输出安全、异常码、链路日志、权限和业务验收用例 | 各工作包 | 安全审计闭环和验收报告 |

研发实施顺序原则：先完成身份权限、会话基础和统一调度，再并行接入智能体、知识、数据和连接器，最后完成配置、审计和综合验收。任何外部接口未具备正式文档和联调环境时，仅可完成接口适配层，不得以演示数据作为验收依据。
